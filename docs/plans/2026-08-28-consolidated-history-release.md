# 后续只读历史集中交付

目标仍是V2全部P4板块可人工逐项测试，不切流、不触发Provider。为减少不必要PR与重复部署，576/119通过exact-main后，120–124使用同一个集中PR：第一批Profile120+HXC121（2包）完成CI和候选Full，但不合并；第二批追加Static122+Customer123+Marketing124（3包），在最终精确SHA重跑CI和候选Full，全绿后才合并，随后exact-main Full绿灯才部署V2。

第一批从已验证dcf28fe创建隔离worktree并内容级接入120/121，保留576的Campaign/Automation文档，OpenAPI/SQLC/Orval与摘要由主代理统一重新生成。随后PR576候选e4813be7与合并e67c5b94树完全相同；exact-main33158720864成功且TARGET/TESTED/commit compatibility均核对e67c5b94后，才将第一批提交rebase到此已验证main。未使用ours丢弃分支改动。

Profile真实隔离30首次/重放/双对账、HXC2476首次/重放/双对账已有证据；它们不替代本集中PR最终SHA的Nightly或正式部署。后续122/123/124已有隔离演练，仍只在第二批加入，不因预先测试而跳过最终候选门禁。

第一批3d29d0d的CI33160391136与候选Full33161217927已成功，TARGET/TESTED/checkout均核对3d29d0d4fee15de9a417ad1d90de6542e20b12be，commit compatibility回写同一run。随后在同一PR577追加Static122、Customer123、Marketing124；按源提交顺序收单，重新生成并测试。119–124九个Store真实PG往返/回滚测试显式接入Full Nightly，避免仅运行默认跳过的opt-in测试。第二批最终CI/候选与exact-main未绿灯前仍不合并/部署。

全部公共契约、迁移序号、路由、生成、CLI仍由主代理串行。叶代理仅交付隔离限定路径的commit。V1绝对只读，历史数据不恢复调度/群发/支付/退款；普通登录与公网入口未闭合，最终部署完成后停在用户人工确认之前。

正式部署优化为119–124一次升级：119已单独通过所有门禁但不单独启动测试运行时；本集中PR最终候选与exact-main全绿后，再从最终main构建、备份、按序迁移119–124并部署。当前正式schema118保持不动，组合恢复已实际验证。用户再次明确全量迁移不缩减、不后置历史；合并部署仅减少重复操作，迁移阶段不夹带无关功能或额外风控体系。
