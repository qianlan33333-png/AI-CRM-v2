# 后续只读历史集中交付

目标仍是V2全部P4板块可人工逐项测试，不切流、不触发Provider。为减少不必要PR与重复部署，576/119通过exact-main后，120–124使用同一个集中PR：第一批Profile120+HXC121（2包）完成CI和候选Full，但不合并；第二批追加Static122+Customer123+Marketing124（3包），在最终精确SHA重跑CI和候选Full，全绿后才合并，随后exact-main Full绿灯才部署V2。

当前第一批从已验证dcf28fe创建隔离worktree，对齐576候选e4813be7（候选Nightly尚在运行，不能把它当已验证main）。内容级接入120/121，保留576的Campaign/Automation文档，OpenAPI/SQLC/Orval与摘要由主代理统一重新生成。生成后的业务树与既有07c5b18f逐文件一致，仅保留前序两份更新文档；未使用ours丢弃分支改动。

Profile真实隔离30首次/重放/双对账、HXC2476首次/重放/双对账已有证据；它们不替代本集中PR最终SHA的Nightly或正式部署。后续122/123/124已有隔离演练，仍只在第二批加入，不因预先测试而跳过最终候选门禁。

全部公共契约、迁移序号、路由、生成、CLI仍由主代理串行。叶代理仅交付隔离限定路径的commit。V1绝对只读，历史数据不恢复调度/群发/支付/退款；普通登录与公网入口未闭合，最终部署完成后停在用户人工确认之前。
