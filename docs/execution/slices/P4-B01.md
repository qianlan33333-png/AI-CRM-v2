# P4-B01：客户读模型旧路由兼容

## 冻结输入与闭环

- Base 为 exact-green `8eab1b7b1be126daf2d0c8f7a8f4ebbcf553c5d1`；从 fresh 隔离
  worktree 实现，旧 B01 WIP 只作史料，未复制或 cherry-pick。
- 仅关闭 `GET /api/customers` (`LEGACY-API-0609`) 和
  `GET /api/customers/{external_userid}` (`LEGACY-API-0619`)。冻结 781 路由账本
  `+2`；293 矩阵和 316 迁移映射均 `+0`，未改动原始账本行或声称 T14 进度。
- 请求、响应和错误证据只读自 immutable legacy snapshot
  `6cb989c071255437d75953dabb943318a74eb8f4`；旧 Python 实现未进入 v2。

## 合法读边界与鉴权

- 0609 继续调用现有 Contact `CustomerListService`；不可安全映射的
  identity 筛选和非零 OFFSET 继续 fail closed `400`。
- 0619 先经 Identity `Resolve` 以
  `wecom_external_userid + wecom-corp:<corp_id>` 解析 OneID，只有 `found`
  才传给 Contact `CustomerDetailService`。`not_found/conflict`均非泄漏地返回 `404`；
  原始 external ID 不进入 Contact 存储或模型。
- 复用 A01 human session、`customers.read` RBAC 和现有 gateway 错误合同。
  admin/ops 为 global，sales 必须将 owner staff 作为 Contact SQL 谓词；无越权降级查询。

## 明确未建设

- 无 DDL、migration、tenant 字段/选择器/索引/权限或跨租户测试。
- 无第二套客户模型、通用 query 平台、新 UI、写操作、同步、队列、
  provider 或真实外部效果。
- 不伪造旧 mobile/binding/owner external ID 字段；响应只输出已有
  Contact 投影与请求中经 Identity 验证的 `external_userid`。

## 验证、修正与外部门

- normal/boundary/error 覆盖 session/RBAC/owner scope、Identity found/not-found/conflict/
  unavailable、Contact not-found/unavailable、无外部调用标志，并复用 Contact/Identity
  既有 PG16.14 验收保证 owner predicate 与 OneID 读取。
- `slice_induced=0`、`infra_induced=0`、`scope_induced=0`、
  `verification_induced=2`：fresh worktree 首次 Orval 生成发现本地未 bootstrap
  pinned 7.21.0，按仓库入口安装后连续生成无漂移；二阶段自审的首个
  禁止模式搜索误扫到 slice card 中的排除项文字，改为仅扫描 Go 业务 diff 后通过。
  `redline=0`。
- `PRODUCTION_DATABASE_NOT_EXECUTED`、`LIVE_MIGRATION_NOT_EXECUTED`、
  `REAL_WECOM_NOT_EXECUTED`、`REAL_SEND_NOT_EXECUTED`、
  `REAL_PAYMENT_REFUND_NOT_EXECUTED`、`DEPLOYMENT_NOT_EXECUTED`。
