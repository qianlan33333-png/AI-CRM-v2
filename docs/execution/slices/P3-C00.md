# P3-C00：冻结 Contact OpenAPI 与公共 Port

## 输入合同

- Base SHA：`3ba8e27f4a26168bce4abd97b2729bc5bed26f97`
- Phase：`P3 / contact wave`
- slice_kind：`central_contract`
- 决策锚点：`USER-CONTINUE-THROUGH-P4-2026-08-12`
- G2 公网 443、浏览器 CRUD 与人工签字继续保留为外部门；用户已明确允许先推进
  开发，不得把这些未执行项写成已通过。

## 冻结范围

1. `customers.id` 继续是渠道中立 OneID；HTTP schema、筛选参数和 contact port
   均禁止 external_userid、unionid、openid、phone 或 mobile。
2. 客户列表只允许 opaque keyset cursor，禁止 OFFSET；冻结 owner、stage、channel、
   tag、删除态、加入时间、最近互动时间与关键词筛选。
3. 列表保留旧系统的总数与 watermark 业务语义；超过 1 万由实现返回估算值并以
   `total_is_estimate=true` 明示，禁止深 COUNT 成为热路径。
4. Contact HTTP 冻结为列表、详情、资料更新、阶段变更、标签增删、时间线和标签
   catalog；所有写操作要求既有 session-bound CSRF。
5. `CustomerEvent.actor` 必填；标签与阶段写入实现片必须与 `event_log` 同事务提交。
6. 跨域 `contact/port.MergePort` 维持 P1 已决三方法，不为 HTTP 或 UI 暴露 store
   细节，也不新增外部身份字段。

## 非范围

- 本片不创建业务表、不实现 handler/store/UI，不连接旧 Python 源码。
- 外部身份筛选属于 identity 归因边界，不得为了照搬旧 mobile/binding 查询把身份
  列放回 customers；对应联合查询在 identity 波次通过已冻结 port/表边界实现。
- 真实企微、生产数据库与 live migration：`NOT EXECUTED / PENDING_EXTERNAL_GATE`。

## 验收

- OpenAPI validator 与永久负例覆盖伪造 P3 evidence、OFFSET、估算标记、actor、
  CSRF、外部身份字段和 contact port 漂移。
- oapi-codegen 与 Orval 连续生成两次无 diff；candidate server 新增 4 个 contact
  operation，runtime server 仍只暴露 health。
- focused tests、全量 Go/Web/repo-contract/secret scan、中文 PR、squash merge与
  精确 main CI。

## 回滚

回滚本片 merge；本片没有 DDL、业务数据或外部效果。

## correction

- `slice_induced=0`
- `infra_induced=0`
- `scope_induced=0`
- `verification_induced=3`：首次从根 module 运行 tools 子模块测试，按模块边界
  改为 `go -C tools`；生成物 manifest 内容更新后同步其冻结摘要，再次运行连续
  生成门通过；完整门首次传入了抄写错误的未解析 head SHA，所有前置门通过后
  query-plan 按设计 fail-closed，改为从 Git 精确读取本片 HEAD 重跑。三项均未
  改变业务合同或降低门禁。
