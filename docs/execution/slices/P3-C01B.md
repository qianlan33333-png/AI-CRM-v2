# P3-C01B：Contact 客户列表 sqlc Store

## 输入合同

- Base SHA：`efe1b961603be8470a595a064b3367c8ef3a0346`
- Phase：`P3 / contact wave`
- slice_kind：`delegated_sqlc_sol_reviewed`
- 决策锚点：`USER-CONTINUE-THROUGH-P4-2026-08-12`

## 完整行为

1. sqlc 查询在同一 transaction-bound context 内完成列表与 bounded total，完整应用
   watermark、全部业务筛选和稳定 `(updated_at DESC,id DESC)` keyset；禁止 OFFSET。
2. 列表只读取 `limit+1` 并在 repository 裁掉探针行；keyset 必须同时提供时间和 ID，
   任一缺失、未来位置或非法过滤 ID 均在访问数据库前 fail-closed。
3. 总数查询不使用 `COUNT`，而是按同一过滤与 watermark 最多返回 10001 个稳定 ID；
   repository 只以返回长度形成 bounded signal，应用服务再显式映射 10k+。
4. customers 读模型保持渠道中立，不新增 external_userid、unionid、openid、phone 或
   mobile；JSON `extra` 在 repository 边界复制，避免复用驱动缓冲区。
5. Sol 在隔离 PostgreSQL 16 增加真实过滤、tag intersection、watermark、同时间 ID
   排序、跨页 keyset、删除态和 10001 cap 验收。

## 委派与复核

- Terra 冻结 base：`4f9a5a51e9d49f4bf3abb06ddeff048306b79e69`
- Terra 最终 head：`22a98b177d390241fcc7e3f85b117fbaa83ee401`
- manifest SHA-256：`040329fc43d9e21d3156db064dce955a756997808c80e6513d4ba504cb99b3aa`
- binary diff SHA-256：`04624a5a2b48da004d515e7235feeed108c285f5d54f792966e271933dcf62a1`
- Sol 已复算 parent、白名单、manifest 与 binary diff，并在最新 main 上集成；Terra 未
  push、未创建 PR、未合并 main。

## 非范围

- 本片不实现 HTTP handler、RBAC 注入、UI 或 20 万性能结论。
- 不连接生产数据库，不部署，不执行真实企微调用或 live migration。

## 验收

- sqlc 连续生成两次无 diff；focused race 与真实 PostgreSQL 16 验收通过。
- query-plan gate 对两条新增 SQL 执行 EXPLAIN，禁止 OFFSET、非 bounded total 和意外
  Seq Scan；随后执行全量 Go/Web/repo-contract/secret scan。
- 中文 PR、squash merge和精确 merge SHA 的 main CI。

## 回滚

回滚本片 merge；本片无 DDL、持久数据写入或外部效果。

## correction

- `slice_induced=1`：Terra 首版以 bounded 子查询包裹 `COUNT(*)`；Sol 复核确认冻结 C-1
  明确禁止 COUNT，退回改为最多读取 10001 个 ID 后计长度，并增加永久静态负例。
- `infra_induced=2`：真实 PG acceptance 首版对 fixture 表的 DML 使用 unqualified 名称；
  ownership lint 正确拒绝后改为显式 `acceptance_fixtures.*`。Terra 的 SQL 静态负例最初
  位于 `internal/contact/store`，source-policy 正确拒绝其中的 SQL literal；Sol 将同一
  断言移入 acceptance 黑盒包，生产 store 继续只调用 sqlc，两个门禁均未放宽。
- `scope_induced=0`
- `verification_induced=4`：新 worktree 第一次 cherry-pick 被 Git 报告本地状态冲突；
  确认 worktree clean、abort 空操作后原提交重试成功。首次 query-plan 调用抄写了错误
  head SHA，改为直接读取 `git rev-parse HEAD` 后 checked=2 通过；首次全量门复用了已迁移
  至 v5 的 public schema，P2 CSRF 故障负例按 search_path 命中 public 同名表；先将隔离
  测试库回滚到空 public schema后再跑。移动静态 SQL 负例时误删 fake tx 仍使用的
  `strings` import，vet 正确拒绝后恢复；均未修改业务代码或降低门禁。
