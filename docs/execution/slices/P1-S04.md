# P1-S04：上层域与剩余旧 API 静态事实

- Issue: #69
- Base SHA: `8b531b95e3a6803f4ed65bcf5773a6803e05d591`
- Executor: Codex Sol；Terra grouped read-only investigation
- Legacy baseline: `6cb989c071255437d75953dabb943318a74eb8f4`
- Slice limit: 2/8 handwritten files，合计不超过 400 行；441 条 route facts 不是 diff 行数

## 单一行为

冻结 P1-S01 中扣除 P1-S02/P1-S03 后严格补集 441 条上层域、平台、Commerce、
Questionnaire、Media、Radar 与 Gateway 路由的静态源码事实、method/owner 差异、状态
语义、外部效果边界和 G1 问题。

## 验收

- 谓词可重放，`156+184+441=781`，overlap/uncovered 为 0。
- 36 个显式 handler 静态对账 441 route decorators；481 个 manifest canonical
  method-path 全覆盖，源码 503 个 method-path 与 22 个辅助 HEAD/OPTIONS 分列。
- registry owner、manifest owner、source container 不静默归并。
- 财务、OAuth、MCP、AI、queue intent 与 provider receipt 分层。
- 所有处置保持 `UNREVIEWED/PENDING_HUMAN_SIGNOFF`。
- 合并门槛：staged repo-contract、永久负例、gitleaks 与 PR/main CI 必须通过。

## 非目标

不修改 OpenAPI、功能矩阵、业务实现、迁移或公共 port；不运行旧 app、数据库、网络、
支付、OAuth、MCP、浏览器、staging、production 或部署。

## 产物

[`api-facts-upper-domains-6cb989c.md`](../../evidence/p1/api-facts-upper-domains-6cb989c.md)
是静态证据，不是 G1 签字或运行验证。
