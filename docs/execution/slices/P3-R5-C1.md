# P3-R5-C1：Identity value boundary only

## 输入与目标

- Base SHA：`37b428d87c13af360d392aa2e1ccfd17075a8a4d`。
- 前置：P3-R4A fixture 与 P3-R4B `00010` storage 均已 CLOSED。
- 目标：以 context-aware、跨 macOS/Linux 的永久夹具保护 raw identity、
  `normalized_value` storage/exact-lookup 与 Resolve fingerprint 边界。

## 允许路径

- `scripts/identityvalueboundary/{main.go,main_test.go}`
- `docs/execution/{slices/P3-R5-C1.md,slice-ledger.yml}`

## 明确排除

- 不改 DDL、ADR、OpenAPI、ports、keyring、runtime 或 Make recipe；repo-contract
  仅维护本片 ledger 的既有 SHA 锚点，不接入 focused checker，接线属于 R5-C3。
- 不实现 receipt/pending/TTL/River/GIN/in-progress 规则；这些属于 R5-C2。
- 不连接生产数据库，不执行 live migration、真实企微或外发。

## 验收与回滚

- 使用 `go run ./scripts/identityvalueboundary -root .` 与
  `go test -race -count=1 ./scripts/identityvalueboundary` 验证。
- fixture 仅用 `t.TempDir`、`filepath` 与 `os.WriteFile`；规则只按生产包、SQL 表/查询
  和 Resolve 请求上下文判定，避免裸 `raw`/`normalized` 全仓误伤。
- 回滚为 revert 本 PR；C2 必须待本片 CLOSED 后才可开始。
