# P3-C01C：Contact 客户列表应用服务

## 输入合同

- Base SHA：`4f9a5a51e9d49f4bf3abb06ddeff048306b79e69`
- Phase：`P3 / contact wave`
- slice_kind：`sol_application_core`
- 决策锚点：`USER-CONTINUE-THROUGH-P4-2026-08-12`

## 完整行为

1. 归一化并校验关键词、owner、stage、channel、tag、删除态、加入时间与最近互动
   时间筛选；默认 50、最大 200，非法输入在事务前 fail-closed。
2. 首屏只读取一次 UTC clock 并固定 watermark；后续页面从 cursor 恢复同一
   watermark，不因 UoW 重跑或翻页时间变化而漂移。
3. cursor 使用无填充 canonical base64url、版本、operationId、固定排序、过滤条件
   指纹和 `(updated_at,id)`，拒绝未知字段、尾随载荷、填充、过滤器复用与未来位置。
4. store 只在 transaction-bound context 内调用；UoW 重跑完整 callback 时查询保持
   确定。应用层校验行数、总数边界、稳定倒序、watermark 和 JSON object 后才返回。
5. 精确总数只到 10000；store 的第 10001 条信号映射为 `total=10000` 与
   `total_is_estimate=true`，不得冒充精确值。

## 非范围

- 本片不实现 SQL、HTTP handler、RBAC 注入或 UI；相应路径由后续独立片闭环。
- 不增加外部身份字段，不读取旧 Python，不连接生产或执行 live migration。
- G2 公网 443、浏览器 CRUD 与人工签字继续为 `PENDING_EXTERNAL_GATE`。

## 验收

- table-driven/race 测试覆盖默认值、全部筛选、非法输入、cursor 严格解码与过滤器
  绑定、固定 watermark、10k+、malformed store 结果、依赖错误与三次 UoW callback。
- focused tests、生成器连续两次、全量 Go/Web/repo-contract/secret scan。
- 中文 PR、squash merge和精确 merge SHA 的 main CI。

## 回滚

回滚本片 merge；本片无 DDL、数据写入或外部效果。

## correction

- `slice_induced=0`
- `infra_induced=1`：P0-S04 历史验收固定占用本机 `5432`，而本期明确要求保留现有
  PostgreSQL、不对其执行破坏性 River up/down/up；本片在隔离 `55432` 完成其余真实
  PG 门，固定 5432 的完整验收留给 PR CI 的隔离 PostgreSQL 16 service。
- `scope_induced=0`
- `verification_induced=3`：首次在建立 staged payload 前运行 repo-contract，门禁按设计
  拒绝 working tree 与 index 不一致；首次全量门遗漏 `P0S03_PG_INTEGRATION`，续跑验收时
  又遗漏 `ACCEPTANCE_FIXTURES_TEST_DATABASE_URL`。补齐隔离 55432 的显式环境后对应真实 PG
  门和全部后续门通过，均未改变代码或降低门禁。
