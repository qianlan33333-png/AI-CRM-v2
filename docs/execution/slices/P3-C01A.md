# P3-C01A：Contact 核心 Schema 与内部查询合同

## 输入合同

- Base SHA：`b77fa4f2df9e5d6d0f56983d42ffb9b9a1616e92`
- Phase：`P3 / contact wave`
- slice_kind：`central_contract`
- 决策锚点：`USER-CONTINUE-THROUGH-P4-2026-08-12`

## 完整行为

1. 在 PostgreSQL 16 创建 contact 独占写入的 `staff`、`channels`、`customers`、
   `tag_groups`、`tags`、`customer_tags`；时间线继续由 C-3 单独创建。
2. `customers.id` 是渠道中立 OneID，表与内部查询合同均不含客户外部身份列。
3. 冻结以 `updated_at DESC, id DESC` 为稳定顺序的 keyset 查询合同；watermark 在
   翻页期间固定，禁止 OFFSET。
4. 总数只精确计算到 10000；第 10001 条仅作为 `10k+` 估算信号。
5. owner/stage/channel/added/last-interact/tag/keyset 与 name trigram 均有索引落点，
   为 C-1B sqlc 实现和 C-6 的无意外 Seq Scan 硬门提供 schema 前提。

## 非范围

- 不实现 sqlc 查询、service、handler 或 UI；C-1B 在本片 merge/main CI 后启动。
- 不创建 `customer_events`，不写 identity 表，不连接旧 Python 或真实用户数据。
- G2 公网 443、浏览器 CRUD 与人工签字继续为 `PENDING_EXTERNAL_GATE`。

## 验收

- 静态合同覆盖表、索引、external identity/offset 永久负边界。
- PostgreSQL 16 真实迁移后核对列、索引和 `pg_trgm`。
- 独立 Terra migration 复核只读 catalog、未读取 migration 源码；最终
  `PASS`，规范化摘要 SHA-256：
  `0dda157f960d712358fdd1b6e76107609eab69a51f4c650717914b29f8b9f9f4`。
- Goose `up/down/up`，sqlc 连续生成、全量 Go/Web/repo-contract/secret scan。

## 回滚

回滚本片 migration；本片只在隔离测试库执行，不进行生产或 live migration。

## correction

- `slice_induced=1`：首轮真实 PG 验收把 `SHOW server_version_num` 的 TEXT 结果直接
  扫进整数；改为先读取字符串再严格解析，未改变 schema 或验收强度。
- `infra_induced=0`
- `scope_induced=1`：独立 migration catalog 复核确认实现满足新 OpenAPI，但旧详细
  设计仍保留早期索引且缺少 `tags.sort_order` 与 JSON object 约束；将同一 schema
  合同同步回详细设计后再复核，不更改波次依赖或运行语义。
- `verification_induced=6`：首次把静态 migration DDL 与 catalog 查询放在同一
  acceptance package，分别触发 ownership 与 source-policy 的正确边界；拆为 contact
  域内静态合同和中央只读 PG 集成验收后，两道门均保持原强度通过。首次全量
  `ci-go` 复用了已迁移到 v5 的库，P2 CSRF 故障负例按 search_path 命中了 public
  同名表；确认 focused 原因后先把隔离库回滚至空 public schema 再跑全量门，未改
  业务代码或降低测试。全量 build 随后准确拦截两个 test-only package 缺少
  非测试 Go 文件，补最小 `doc.go` 后保留原测试布局和所有门禁。
  随后 release archive 门按设计拒绝未提交的 staged index；先建立本片本地提交再以
  精确 base/head 重跑全量门，没有跳过 release cleanliness。Web 首轮命令把 Node
  自带 npm 路径放在固定 npm 11.12.1 之前，version gate 正确拒绝；调整 PATH 顺序后
  81 项测试、build 与 audit 全绿，未改前端代码。
