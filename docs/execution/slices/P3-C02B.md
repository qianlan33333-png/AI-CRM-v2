# P3-C02B：Contact 客户详情读取

## 输入合同

- Base SHA：`96f93f17662c00987be0edf3d4f8059ae61bd477`
- Phase：`P3 / contact wave`
- slice_kind：`sol_complete_api_operation_with_terra_mechanical_store_and_tests`
- 决策锚点：`USER-CONTINUE-THROUGH-P4-2026-08-12`

## 重新划片

P3-C02A 的 `slice_induced_correction_count=3`，本片按 AGENTS.md §4 回退规模，
只闭环 `getCustomer` 一个 API operation。时间线、全量标签目录及四个 mutation
HTTP operation 分片后移，不在本片捆绑。

## 完整行为

1. `GET /api/v1/customers/{customer_id}` 返回渠道中立 Customer 与该客户已应用的
   本地标签；不返回企微标签 ID 或任何 external identity。
2. admin/ops 使用 global scope；sales 必须把自身 `owner_staff_id` 下推为 SQL
   谓词。越权与不存在都返回稳定 404，禁止泄露客户存在性。
3. 客户与标签在同一只读 UoW 快照中读取；store 必须使用 transaction-bound
   context。应用层拒绝非法 customer、重复标签、owner 逃逸与不稳定排序。
4. 标签固定按 `(group_sort_order, sort_order, id)` 升序，group 与 group_name
   必须同时存在或同时为空；返回数组不得为 null。
5. HTTP 层只使用冻结 generated wrapper，响应继续复用严格 Customer 转换和统一
   no-store/error envelope。

## 非范围

- 不实现 `listCustomerEvents`、`listTags` 或任何写 operation；不接 UI。
- 不修改 OpenAPI、migration、公共 contact port 或鉴权模型。
- 不连接生产数据库，不执行部署、live migration 或真实企微调用。

## 委派白名单

- Terra 可在本卡与 `CustomerDetailStore` 冻结后实现 sqlc query/store 与对应单测，
  或扩充 service/handler 测试语料；不得修改 OpenAPI、migration、公共 port、
  黑盒 acceptance、运行时装配、中央契约与治理文档。

## 验收

- focused race；真实 PostgreSQL 16 验证 global/owner/not-found、标签排序和空数组。
- sqlc 连续两次无 diff；Go/Web/repo-contract/secret 全门。
- 中文 PR、squash merge、精确 merge SHA main CI。

## 回滚

本片无 DDL 和写入；回滚 merge 即可。
