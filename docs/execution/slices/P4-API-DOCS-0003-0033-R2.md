# P4-API-DOCS-0003-0033-R2

## 目标

从 exact-green main `d81ba86bde4752fe6d3e402f7bf5bebf007db600`（base override 决策
`P4-API-DOCS-0003-0033-R2-BASE-OVERRIDE-2026-08-18`）恢复两条完整 API 行为：
`GET /admin/api-docs`（`LEGACY-API-0003`）与 `GET /admin/config/mcp-tools`（`LEGACY-API-0033`）。

## 冻结合同

- owner：`cmd/aicrm` composition-root 兼容适配器；唯一数据源为编译期嵌入的 canonical
  `api/openapi.yaml`（启动解析一次、fail-closed），禁止运行时枚举 router。
- 身份与权限：human session + `config.overview.read`，admin global；GET 不要求 CSRF。
- 0003：200 HTML，带 no-store / nosniff / no-referrer / CSP（至少 `connect-src`、
  `form-action`、`base-uri` 均为 `'none'`）；十五组固定分组、快速索引、浏览器本地
  substring 搜索（仅 method+path+summary）、Markdown 复制（`ep:`/`group:`/`full`）、
  hash 导航；禁止展示 examples、default、server URL、函数名、源码行号、raw vendor
  扩展、凭据值、合成请求响应、curl 及旧误导文案。
- 0033：授权后精确 302，`Location` 精确 `/admin/api-docs`，query 丢弃、body 不解析、
  不记录、不回显，空响应体，no-store。
- 0034 `POST /admin/config/mcp-tools/save` 保持 `DEFERRED_POST_LAUNCH`，本片不得注册、
  不得改 disposition、不得计入进度，并有静态测试 guard。
- 非 GET 方法由 Chi 在认证前返回 405。
- 无 OneID、tenant、migration、UoW 业务写、event、receipt、provider、URL/blob 读取或外部效果。

## 完整分母

- A：`LEGACY-API-0003`、`LEGACY-API-0033` 两条 API 映射。
- B：`LEGACY-S05-059` 一行 Feature Matrix（本片片内唯一可关闭行）。
- migration：零。
- migration closure：`no_schema_or_external_effect`；本片没有新增 schema、migration 或外部效果。
- deferred：`0034` 保持 `DEFERRED_POST_LAUNCH`，不属于本片完成数。

## 验收

- builder 单测、handler 测试、注册级黑盒（401/403/405/302/404）与 0034 静态 guard 均须通过。
- headless 浏览器冒烟：页面外壳、Markdown 复制三类、本地搜索、hash 展开、无二次网络请求。
- canonical OpenAPI 与 Orval 生成连续运行无 diff；API Mapping、Feature Matrix 与 repo
  fingerprints 同步。
- 必跑：focused + race、`go test ./...`、selected go gates、api codegen 检查、browser
  smoke、repo-contract、secret scan 和 protected-main 四项 Required Checks。
- staging、production、真实企微/支付/外发均 `NOT_EXECUTED`。

## Exact-main closure

- reviewed head：`c1370ef1115e88f5ac9fb82c8aa808283402d671`
- protected PR：`#248`
- sole required `ci / merge-gate`：PASS（run `32125106786`）
- match-head squash merge：`98ab41477cfa266114c170031d6c42c9c83ba980`
- reviewed-head tree 与 merge tree：byte-identical
- 状态：exact-main CLOSED，not DEPLOYED；staging/production/真实外效均 `NOT_EXECUTED`
