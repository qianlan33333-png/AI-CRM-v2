# P0-S01 acceptance evidence

状态：`CODEX_ACCEPTED_LOCAL_PENDING_PR`

证据层级：`LOCAL SKELETON`。本 Slice 没有 HTTP、数据库、River、staging、
production 或真实企微验证，不能描述为可服务应用或已部署。

## 输入 provenance

- base：`main@7ae62e2aed112d70341b6ea435bb08e7eac80de7`
- branch：`slice/p0-s01-go-role-startup`
- [Issue #5](https://github.com/qianlan33333-png/AI-CRM-v2/issues/5)
- [当前 ChatGPT Pro 对话](https://chatgpt.com/c/6a772a78-5d70-83ee-9d4a-816717d023d5)
- [按用户指令失效的首次对话](https://chatgpt.com/c/6a77278d-24a4-83ee-9f01-515784ff49e5)：
  其输出从未作为本 Slice 交付来源
- 输入 ZIP：`65,763 bytes`
- 输入 SHA-256：
  `d318ce5b2082e33c6ce50501e490fbd3349033d9359875acf5f4e6619922a7af`
- gitleaks：`8.30.1`，全历史与 payload 均 PASS

输入包由 base 的白名单 Git blobs 构成，不含 `.git`、`.github`、依赖、缓存、
数据库、运行状态、浏览器状态、symlink/special、敏感路径或凭据模式。普通无
`.git` 解压目录中的 `make generate-check` 与 `make mod-check` 均真实通过。

## Pro 输出与两次纠错

### 第一候选：拒收

第一候选供应链与 Go 1.26.5 基础门通过，但 Codex 额外对抗测试确定复现：

1. `errors.Join(context.Canceled, cleanupErr)` 被当成 benign，`Run()` 实际返回
   `nil` 并吞掉真实 cleanup error；
2. Worker 报错、API 卡住超时时，文本为 Worker error 后接 API timeout，违反
   固定 API → Worker 顺序；
3. 原顺序测试在 API 文本缺失时可能因 `strings.Index == -1` 假通过。

第一候选的 5 个附件 hash 均已核实，拒收原因与日志见
[Issue 纠错记录](https://github.com/qianlan33333-png/AI-CRM-v2/issues/5#issuecomment-5226339657)。
对抗日志持久保存在仓库外 `handoffs/P0-S01-adversarial-results-7ae62e2/`；
其结果是 synthetic/local process evidence，不是 staging 或 production evidence。

### Correction 1：实现修正

Pro 在原对话最小修正 `run.go` 与 `runtime_test.go`：

- 只有 error tree 的全部非 nil 叶子都是 `context.Canceled` 才视为 benign；
- mixed cancellation + cleanup error 保留真实 error；
- timeout 写回固定 slot 后按 API → Worker 聚合；
- 顺序断言先验证 `errors.Is` 与两个文本均存在，再验证顺序。

### Correction 2：同名附件绑定修正

修正版同名附件按钮中，Patch、tests.log 与 SHA256SUMS 仍下载为第一候选旧文件；
Codex 以实际 bytes/hash 拒绝混用。Pro 未再修改代码，只重新提供唯一命名外层包
`P0-S01-r2-delivery-067433e8.zip`。

最终外层包：

- bytes：`14,747`
- SHA-256：
  `2548cdd632a9832d17ae3edb4cae4aa072c7f6363bf6f3bf7e8aa5ed6ca09a9b`
- 内容：恰好 5 个 root regular `0644` 文件、0 个目录，无加密、路径穿越、
  重复、大小写冲突、symlink/special 或敏感内容

最终内层交付：

| 文件 | Bytes | SHA-256 |
|---|---:|---|
| `P0-S01.patch` | 13,108 | `067433e8afcf6d159aa621ffbaaafe00272c1b2e7202fcee2edea5e8a66b8d1c` |
| `P0-S01-changed-files.zip` | 4,775 | `4fbd6ab1225d6d9c4902074704bf497cb84a1453004b7c5acb26654360fa5d66` |
| `P0-S01-report.md` | 8,921 | `7a5919d89b1c278ca80a699968fc7fd15dc85c7bc6f37589db881cec8f22a5b2` |
| `P0-S01-tests.log` | 6,445 | `53c82a2bda1213e7efefe798bc002ce5dba454e4fccfe8dca764ff5f9e5baff5` |
| `P0-S01-SHA256SUMS` | 383 | `d7ae9b949128d7ad3fa488097f97399780d390e93ac4449ddafe8e3690957dc9` |

最终 Patch 仅含 5 个白名单文件，`397 additions / 0 deletions`；在 detached
`7ae62e2aed112d70341b6ea435bb08e7eac80de7` worktree 中 `git apply --check`
与应用均通过，应用结果和 changed-files ZIP 五文件逐字节一致。没有依赖、锁文件、
中央合同、ADR、OpenAPI、migration 或生成物变化。

## Codex 独立验收

环境：`go1.26.5 darwin/arm64`，`GOTOOLCHAIN=local`、`GOWORK=off`、
`GOFLAGS=-mod=readonly`。以下均在最终 Patch 应用后的精确 base worktree 执行：

| 门禁 | 结果 |
|---|---|
| `go test -race ./internal/platform/runtime` | PASS，exit 0 |
| `go test -race -timeout=15s -tags=p0s01_acceptance ./acceptance/p0s01` | PASS，exit 0 |
| `acceptance/p0s01/process_blackbox.sh` | PASS，exit 0 |
| `go test -race ./...` | PASS，exit 0 |
| `go vet ./...` | PASS，exit 0 |
| `go build -trimpath ./cmd/aicrm` | PASS，exit 0 |
| `make generate-check` | PASS，exit 0 |
| `make mod-check` | PASS，exit 0 |
| `make vuln` | PASS，exit 0；reachable vulnerabilities 为 0 |
| `make ci-go` | PASS，exit 0 |
| gitleaks 8.30.1 + 自定义敏感内容扫描 | PASS，exit 0 |

`govulncheck` 另报告 imported packages 中 4 个、required modules 中 21 个未被本代码
调用的漏洞；本 Slice 没有新增依赖，不能把该信息省略或改写成依赖树零漏洞。

额外对抗门均通过：

- fast lifecycle race `-count=20`；
- direct、wrapped、joined cancellation-only error tree；
- mixed cancellation + cleanup error 保留；
- 真实公共 10 秒 timeout 与 API → Worker 顺序；
- 精确 CLI bytes、三 role × SIGINT/SIGTERM；
- component panic 不 recover。

这些对抗测试是本地 synthetic process validation。未模拟或声称真实生产验证。

## 实际代码范围

- `cmd/aicrm/main.go`：严格 CLI、稳定退出码、SIGINT/SIGTERM 父生命周期；
- `cmd/aicrm/components.go`：仅等待 context 的 API/Worker placeholder；
- `internal/platform/runtime/cli.go`：六种合法 role 写法与 fail-closed 参数分类；
- `internal/platform/runtime/run.go`：原子预检、并发 slot、取消、固定错误顺序和
  10 秒 graceful timeout；
- `internal/platform/runtime/runtime_test.go`：合同单元测试与两项纠错回归。

本片没有 HTTP、`/healthz`、DB、River、配置、业务模块、外部调用或 readiness
声称。

## 待完成与权限边界

- 中文 PR、远端 application/repo-contract/secret-scan CI：`PENDING`
- squash merge 和 merge SHA：`PENDING`
- staging、production、真实数据库、真实企微、部署：`NOT_AUTHORIZED`
