# P3-S02：Segment QueryProgram compiler

## 输入合同

- Base SHA：`861447ec652ce565edb8e79b274fb098ba23e81a`（P3-S01 exact-main CLOSED）。
- 总指挥 2026-08-13 Decision B：在现有索引与 S02 禁止 DDL 的边界下，不能将任意
  `and/or` AST、固定 sqlc 查询族和 generic-plan 零 Seq Scan 同时成立；S02 因此只产出
  QueryProgram，S03 独占 index contract、固定 sqlc 查询与 executor。
- 只实现 `internal/segment/compiler` 和本卡/ledger/永久契约保护；不改 migration、Contact、
  Identity、OpenAPI、sqlc、store、PG query 或生成物。

## 可观察行为

- `Compile(dsl.AST, referenceUTC)` 将已验证 S01 typed AST 映射为不可变、确定性的闭集
  QueryProgram；其 canonical JSON 明示 `universe=all`，保留 group 遍历顺序与 bind 顺序。
- leaf opcode 只覆盖 S00 白名单的 stage/owner/channel/tag/added/last-interact/deleted 语义；
  bind 只可能是 positive integer、sorted positive integer list、absolute UTC timestamp 或
  boolean。relative day 在单次 UTC reference 下解析为 absolute timestamp。
- QueryProgram 只有 AND/OR 集合节点和闭合 leaf；DSL v1 禁止 `not`，故本片不虚构反集。
  S03 对 all-customers universe 执行固定 leaf 查询与集合代数，必须保留 `is_deleted=true`
  的合法语义。
- 未知 field/operator/value type、非法 AST 或非 UTC/zero reference fail-closed，只返回
  `segment_compile_unrepresentable` 或 `segment_compile_unsafe`；无 raw SQL、identifier、
  string bind 或 escape hatch。

## 验收矩阵与硬边界

- 表驱动 50 组覆盖所有 opcode、布尔双值、absolute/relative time、and/or nesting；相同
  canonical AST 必须产生相同 Program，恶意输入仅能成为 typed bind，不能形成 SQL 文本。
- property/fuzz：任意可解析 canonical AST 的编译不 panic、Program 规范序列化稳定且无
  SQL-shaped text。
- 不新增 DDL、customers 查询、sqlc 生成物、PG EXPLAIN、executor、物化、调度、HTTP、UI
  或第二份 filter-to-SQL 实现。S03 前真实三人群包对拍仍为 `PENDING_EXTERNAL_GATE`。

## 规模与外部门

- 手写业务范围：`internal/segment/compiler/*.go`；本卡、S00 分层裁决、ledger 与永久门禁为
  治理证据，不计业务实现范围。
- 不连接生产数据库、不执行 live migration/cutover、不发送外部消息。
