# P1-S08：功能矩阵结构、证据与完成度门

- Issue: #77
- Base SHA: `84b893aef66f8be0074b25894debb95bbbdd975c`
- Executor: Codex Sol；Terra Ultra independent red-team review

## 单一行为

让 [`docs/feature-matrix.csv`](../../feature-matrix.csv) 成为可重放的 ADR-010 合同：基础 CI
验证 17 列 schema、状态、引用和证据一致性；独立 completion gate 如实报告阶段待办。

## 验收

- 冻结当前 293 行的 ID 顺序及九个旧行为事实字段；删行、重排、重编号或静默弱化必须失败。
- 基础门拒绝重复 ID、非法枚举、悬空/自引用 target，以及状态与证据矛盾。
- `IMPLEMENTED` 必须同时引用 PR、40 位 merge SHA、测试和实现路径；synthetic、staging、
  production 分别使用不同证据字段，production 还必须记录授权。
- P1/P4/P5 completion 分层；当前 293 行均诚实返回 PENDING，不进入基础 `ci-go` 失败条件。
- Gitless/Bash 3.2 永久负例、staged repo-contract、gitleaks、PR CI 与合并后 main CI 必须通过。

## 非目标

- 不改变矩阵中的业务事实、状态或人工判断，不代替逐页面真实验收。
- 不运行旧 app、浏览器、数据库、provider、staging、production、部署或真实迁移。
- 仓内 hash/负例是 PR/main CI 的审计材料，不宣称能取代外部审查或 branch protection。
