# P2-12：React Web shell、布局与前端路由

## 输入合同

- Base SHA：`17ad04468478abc2cf2d844a953ab4e39122d3fc`
- Phase：`P2`
- 依赖：P2-11 / PR #109 已 squash merge，精确 main CI 全绿
- 决策锚点：执行计划 §3.1 D 组、详细设计 §4.2/§8.1、P2-12 用户确认路线
- 执行：Sol 冻结路由、可访问性、边界与验收；Terra Max 仅在
  `web/src/main.tsx`、`web/src/main.test.tsx`、`web/src/shell.css` 实现已冻结 UI，
  仅本地 commit 并提供 hash 收据；Sol 独立复核、门禁、PR 与合并

## 完整行为

1. 建立不依赖新根依赖的 History API 前端路由：精确支持 `/`、
   `/customers`、`/stages`、`/segments`、`/outbound`、`/settings`；未匹配
   pathname 显示统一 404 页。query/hash 不参与 route key，路由不使用
   prefix/default 推断。
2. 同源站内导航拦截普通左键点击，调用 `history.pushState`并派发
   route change；保留 modifier key、非左键、`target` 与站外链接的浏览器
   原生语义。`popstate` 能驱动 back/forward 重新渲染，卸载时清理监听。
3. shell 提供 skip link、product header、可注入的 navigation 区、有唯一
   `h1` 的 main content 与环境状态区；完整键盘 focus、窄屏和
   `prefers-reduced-motion` 退化。视觉定向为“编辑感运营指挥台”：暖色
   纸感底、深墨色结构、朱红小面积强调，不使用外部字体/图片或
   紫色渐变模板。
4. 本片的六个 route 页面只表达模块边界与实现状态，不伪造业务
   数据、已实现 API 或浏览器验收。`/stages` 为 P2-17 预留稳定
   route，其余业务页由后续切片替换占位内容。

## 验收

- 单元测试覆盖六个精确 route、404、query/hash、站内点击、modifier/
  external 保留、popstate、监听清理、landmarks、skip link 与唯一 `h1`。
- `npm run lint/typecheck/test/build/audit`；连续两次 Orval 无 diff。
- 新 shell 源码、样式、测试、切片卡与 Terra 收据全部进 repo-contract
  hash 门禁，并配永久负例。
- 全量 Go CI、Web CI、repo-contract、secret scan、中文 PR、squash merge 与
  精确 main CI。

## 明确不做

- 不实现登录页、session 路由守卫、退出、权限菜单、侧边栏数据或
  60 秒缓存；这些属于 P2-13。
- 不实现 stages CRUD 或任何 P3 业务页，不修改 OpenAPI、公共 port、
  根依赖、鉴权/secret 边界。
- 不连接生产数据库、不部署、不调用真实企微、不执行 live migration。

手写规模目标低于 P2 `12 files / 800 lines`；测试、文档、收据与 ledger
按既定口径排除。

## 修正记录

- `slice_induced=0`、`infra_induced=0`、`scope_induced=0`、
  `verification_induced=0`；Terra correction 待收据。

## 回滚

回滚本片 squash merge，Web 恢复 P0 的单页占位骨架；P2-11 的 API/
session/RBAC 合同与数据均不受影响。
