# P1-S06：企微、人群与外发前端行为候选矩阵

- Issue: #73
- Base SHA: `63fc09c114939890f4f24eb27cb2c9bcba2aab9c`
- Executor: Codex Sol；Terra Ultra grouped read-only investigation
- Legacy baseline: `6cb989c071255437d75953dabb943318a74eb8f4`

## 单一行为

向 [`docs/feature-matrix.csv`](../../feature-matrix.csv) 追加企微渠道、人群包、Group Ops、
User Ops 与 external-effect 控制面的 44 个前端/API 边界候选。

## 验收

- 新增稳定 ID `LEGACY-S06-001` 至 `LEGACY-S06-044`，且 P1-S05 的 66 行逐字不变。
- 新增行状态统一为 `UNREVIEWED/NOT_STARTED/NOT_RUN/PENDING_HUMAN_SIGNOFF`。
- durable callback ACK、preview、queued、accepted、pending_review/draft、provider receipt 与
  真实送达严格分层；`external_userid` 不得冒充 v2 OneID。
- 3 个手写文件、手写 diff 不超过 400 行；staged repo-contract、永久负例、gitleaks、
  PR CI 与合并后 main CI 必须通过。

## 非目标

- 不修改 OpenAPI、业务实现、迁移、公共 port 或 P1-S05 既有行。
- 不运行旧 app、浏览器、数据库、网络、真实企微、staging 或 production。
- 跨 Slice API 调用只记录前端依赖，不改变 P1 API 分区。

## 证据

[`pages-wecom-audience-outbound-6cb989c.md`](../../evidence/p1/pages-wecom-audience-outbound-6cb989c.md)
记录静态范围、计数、外部效果边界与 G1 待签字项。
