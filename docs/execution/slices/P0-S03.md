# P0-S03：sqlc 最小查询适配器

状态：`BLOCKED — READY_AFTER_P0-C05B_MERGE`

执行模式：`internal_terra`

执行器：`ROOT_DISPATCH_PENDING`

执行器模型：`gpt-5.6-terra`

推理：`ultra`

派发任务 ID：`ROOT_TASK_ID_PENDING`

独立 worktree：`ROOT_WORKTREE_PENDING`

实现基线：`P0_C05B_MERGE_SHA_PENDING`

## 目标与边界

本 Slice 只新增一个无状态适配器，形成“手写 adapter → sqlc `Querier` → PostgreSQL
生成查询”的闭环；不是 health/readiness、DB 初始化、pool、事务、迁移、HTTP 或
调度。完成仅能声明 local/synthetic 验证，不能声明预发、生产或真实数据。

权威输入：`AGENTS.md`、`sqlc.yaml`、`internal/platform/store/generated/{db.go,querier.go,health.sql.go}`、
`internal/platform/store/contract.go`、`acceptance/p0s03/query_contract_test.go` 与本卡；冲突即停止并报告。

## 唯一允许改动

只可新增 `internal/platform/store/ping.go` 和 `ping_test.go`；`package platformstore`，
零新增依赖，总手写行数最多 220。不得修改已有文件、生成物、查询、sqlc 配置、
migration、依赖、共享 CI、checker、公共 port 或本验收夹具。

`PingStore` 唯一字段为 `dbgen.Querier`：

```go
type PingStore struct { querier dbgen.Querier }
func NewPingStore(db dbgen.DBTX) *PingStore
func (store *PingStore) Ping(ctx context.Context) error
```

构造器必须以 `dbgen.New(db)` 建立该字段。`Ping` 只调用生成的 `Querier.Ping(ctx)`：
返回值 `1` 时成功；查询错误直接返回，使 `errors.Is` 可见；其他值精确返回
`fmt.Errorf("platform store ping: unexpected value %d", value)`。不增加其他导出符号、
nil DB 语义、重试、timeout、日志、metrics 或 transaction。

禁止手写 SQL、直接 `Query`/`QueryRow`/`Exec`、env/settings、连接或 pool、transaction、
migration、HTTP/listener、River、goroutine、timer；也不得触碰 `/healthz`。

## 测试与验收

先让 `ping_test.go` 在无实现时编译失败；它至少覆盖生成值 1、底层错误的
`errors.Is` 与值 0 的精确错误。中央 tagged 验收已冻结相同三行为，并只在下列
显式开关、完全相等的 loopback `aicrm_test` DSN 下连接 PostgreSQL：

```bash
P0S03_PG_INTEGRATION=1 \
P0S03_TEST_DATABASE_URL=postgres://postgres:postgres@127.0.0.1:5432/aicrm_test?sslmode=disable \
GOFLAGS=-mod=readonly go test -race -tags=p0s03_acceptance ./acceptance/p0s03
```

验收会拒绝缺开关或不同 DSN，且错误不得回显开关值或 DSN；绝不读取 `DATABASE_URL`；连接后断言
`server_version_num=160014`，先运行真正的 `dbgen.New(conn).Ping`，再运行适配器。
实现完成后记录 focused race、tagged acceptance、普通 race/vet/build、generate、mod、
vuln 的完整输出和退出码；未运行标记 `NOT EXECUTED`。

静态扫描、gitless fixture 及共享 CI/Makefile 接线由依赖 Slice `P0-C05B` 冻结；
本 Slice 必须等待其合并并由 root 将上述基线替换为精确 merge SHA。执行器仅在派发的
独立 worktree 工作，不执行 Git、GitHub、部署、迁移、外部调用或凭据读取。
