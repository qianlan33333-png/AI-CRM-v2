// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.28.0
// source: local_lifecycle.sql

package productdb

import (
	"context"
)

const deleteLocalProductIfSafe = `-- name: DeleteLocalProductIfSafe :one
WITH command AS (
  SELECT $1::jsonb AS payload
), doomed AS (
  DELETE FROM products AS product
  USING command
  WHERE product.id = (command.payload->>'product_id')::bigint
    AND product.version = (command.payload->>'expected_version')::bigint
    AND product.local_lifecycle = 'draft'
    AND NOT EXISTS (
      SELECT 1
      FROM product_local_entitlements entitlement
      WHERE entitlement.product_id = product.id
    )
    AND NOT EXISTS (
      SELECT 1
      FROM coupon_targets coupon_target
      WHERE coupon_target.product_id = product.id
    )
    AND NOT EXISTS (
      SELECT 1
      FROM order_list_projections order_projection
      WHERE order_projection.product_id = product.id
    )
    AND NOT EXISTS (
      SELECT 1
      FROM service_period_member_views member_view
      WHERE member_view.service_product_id = product.id
    )
    AND NOT EXISTS (
      SELECT 1
      FROM service_period_member_grid_collaborators collaborator
      WHERE collaborator.service_product_id = product.id
    )
  RETURNING product.id
), counter AS (
  UPDATE product_catalog_counters
  SET total_products = total_products - 1
  WHERE singleton = TRUE AND EXISTS (SELECT 1 FROM doomed)
  RETURNING total_products
)
SELECT count(*)::bigint AS deleted_count
FROM doomed
`

// Only a draft with no local entitlement, order projection, or other Product
// fact may be physically deleted. Product images may cascade with the draft;
// financial/order/entitlement facts never do.
func (q *Queries) DeleteLocalProductIfSafe(ctx context.Context, command []byte) (int64, error) {
	row := q.db.QueryRow(ctx, deleteLocalProductIfSafe, command)
	var deleted_count int64
	err := row.Scan(&deleted_count)
	return deleted_count, err
}

const lockLocalProductDeleteReferences = `-- name: LockLocalProductDeleteReferences :exec
LOCK TABLE coupon_targets, order_list_projections IN SHARE MODE
`

// SHARE conflicts with the ROW EXCLUSIVE lock acquired by INSERT/UPDATE on
// both reference tables. The FK added by migration 00059 is the final
// product-row integrity check after this deterministic table-lock window.
func (q *Queries) LockLocalProductDeleteReferences(ctx context.Context) error {
	_, err := q.db.Exec(ctx, lockLocalProductDeleteReferences)
	return err
}

const updateLocalProductLifecycle = `-- name: UpdateLocalProductLifecycle :execrows

WITH command AS (
  SELECT $1::jsonb AS payload
)
UPDATE products AS product
SET legacy_admin_projection = command.payload->'legacy_admin_projection',
    local_lifecycle = command.payload->>'local_lifecycle',
    updated_at = (command.payload->>'updated_at')::timestamptz,
    version = product.version + 1
FROM command
WHERE product.id = (command.payload->>'product_id')::bigint
  AND product.version = (command.payload->>'expected_version')::bigint
`

// Product-local WeChat-pay lifecycle queries. The command payload is a
// closed JSON object produced by the Product app port; it is not a browser
// contract and contains no provider token or payment request.
func (q *Queries) UpdateLocalProductLifecycle(ctx context.Context, command []byte) (int64, error) {
	result, err := q.db.Exec(ctx, updateLocalProductLifecycle, command)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}
