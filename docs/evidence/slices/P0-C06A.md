# P0-C06A：River core contract replay

状态：`PR_CI_VALIDATED — PENDING_MERGE`；P0-S04 仍为 `BLOCKED_ON_P0-C06B_GATES`。

- `execution_mode: internal_terra`
- task：`/root/p0_c06a_river_contract_replay`；model：`gpt-5.6-terra`；reasoning：`ultra`。
- worktree：`/Users/qianlan/Downloads/新CRM/handoffs/terra-worktrees/P0-C06A-R1`；branch：`codex/p0-c06a-river-contract`；base：`511bf61f4d553d1e5f6bd3f82edc00f6f8fdc9f4`。
- C06B dispatch base：`P0_C06A_MERGE_SHA_PENDING`；P0-S04 implementation base：`P0_C06B_MERGE_SHA_PENDING`。
- `correction_count=2`；`correction_task_ids=[/root/root_stage_audit, /root/receipt_consistency_audit]`。

本片只包含 root River/`riverpgxv5` `v0.24.0` pin、`platformriver` typed contract、future-adapter tagged acceptance 和执行记录。`pgx v5.7.5`、chi `v5.2.3`、oapi runtime `v1.2.0` 保持不变；无 replace、blank import 或 pin hack。没有 `Runtime`、`NewRuntime`、`Migrate` 实现、River internal import、DDL、scheduler/job/timer/cmd/API 接线或自动迁移。

本机已有 module cache 离线提供官方公开 surface；`v0.24.0` peeled `3cfef1a63d8dc58b795d8a3958ba5d36cf02a217` 只作为冻结历史证据，不代表本次联网核验。tagged suite 现应在 `Runtime`/`Migrate` 未定义处编译失败；因此不会到达 env gate 或连接 PG。无 `ALLOW_DESTRUCTIVE_RIVER_MIGRATION_TEST=1` 时，未来真实迁移门必须明确报告 `PENDING_EXTERNAL_GATE`。

Terra 离线层的 `gofmt`、tidy diff、vet、race、build、generate/mod check 通过；其 `make ci-go` 曾因禁网下本地 vuln DB 不可用而 BLOCKED，仅为本地层级记录，不是最终状态。payload gitleaks 8.30.1 与既有 index custom scan 通过。

root staged receipt：core manifest `50764de4078f421643fc1ffb125921c66ed6e388b02efe4f7cddeebc9f2d683a`；canonical binary diff `16f67419f862233312842b370e9fdef6aad6516457d5c64ac1390fc764fc079f`；pre-receipt staged tree `213df6c037f9441e1840d5118536d67f127eb887`。main-base staged numstat（排除 go.sum）为 `352 additions + 16 deletions = 368`，替代此前本地 snapshot 数字。

Issue #21 / PR #22（head `56330e83380798e36a4b258a50f9b99f53302943`）：application SUCCESS `31277793339/job93154114754` 验证 River v0.24.0 下载、ordinary `internal/platform/river` compile、govuln no reachable 与既有 PG160014 Goose baseline；P0-S04 tagged/adapter/River migration 均 `NOT_WIRED/NOT_EXECUTED`。repo SUCCESS `31277793349`；secret SUCCESS `31277793351`。
root stage 的 repo-contract/permanent negatives、gofmt/tidy、完整 `make ci-go`（含在线 govulncheck，0 reachable）、gitleaks history/tree、sensitive 与 bundle 均 PASS。tagged acceptance 仍为预期 exit 1，仅 9 处 `Runtime`/`NewRuntime`/`Migrate` undefined，编译期未触 PG。adapter、PG、C06B、merge/main CI 与 deploy 仍 pending；不得把 staged 或 PR CI 验收误报为这些外门通过。
