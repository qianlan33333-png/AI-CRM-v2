# P3-C07B1：Contact lineage-aware timeline 行为闭环

## 输入合同

- Base SHA：`cefaf5fd35ff7b7154b9b769fa859a496f042713`
- Phase：`P3 / contact lineage timeline behavior`
- dependency：P3-C07A 已合并且 `customer_merge_lineage` 归 contact owner；
  C07B0 / C02D fixture PR #144 已 squash merge；本片在 S01 合入后的 exact
  main 上重放；共享 infra cleanup 修复合入后的 exact-main application/go、
  repo-contract、secret-scan 均成功。
- supersedes：原 P3-C07B builder 任务 `019ff61a-73b3-7291-a4de-7d5f352580a8`
  已在 `slice_induced_correction_count=3` HARD STOP；其 dirty WIP 只读，
  禁止继续编辑、提交或发布，历史计数不得清零或改归因。
- 原 staged WIP 已以 binary patch、完整 SHA-256 与本地保护提交保全；本片在
  exact main 上独立 clean replay，仅重放冻结的行为改动。

## 唯一可观察行为

1. 给定未删除的有效 root，并在 owner scope 存在时于同一 SQL
   验证 root owner，递归解析 `customer_merge_lineage` 的 root 与全部
   descendants。
2. 合并后时间线全局按 `(occurred_at DESC, id DESC)` 稳定 keyset
   分页；查询只取 `limit+1`，禁止 OFFSET 与 COUNT。
3. 返回事件的原 `customer_id`、`payload`、`actor`、`occurred_at` 与事件
   ID，不把 descendant 改写为 root。
4. 直接读取已归并 customer ID、owner mismatch、未存在 ID 统一
   not-found，不泄露谱系或归属。
5. repository 只从 caller transaction context 取 sqlc query，禁止内部
   新建 transaction 或 UnitOfWork。

## 数据与排除边界

- 仅使用 C07A 已合并 schema；本片无 DDL、migration 或索引变更。
- 明确排除 EXPLAIN、index-plan 强制证明和性能调优（P3-C07B2）。
- 排除 P3-C07C external registry / `AppendExternalEvent`、Identity、Segment、
  UI/OpenAPI、C07A 建档或合并写行为重写。
- 不连接生产数据库，不执行真实企微、真实迁移或 cutover。

## 验收

- focused app/http/store race 通过，并保留无效行与事务边界 fail-closed。
- PostgreSQL 16.14 专属 55432 真实行为测试覆盖 root + descendants、
  至少三页、同时间 ID 降序、无重复/无遗漏、原事件保真、
  merged 直读与 owner mismatch not-found。
- sqlc 连续生成两次无 diff；repo-contract 正例与永久负例全绿。
- 一次必要完整本地门在 clean candidate commit 上执行。

## 修正与外部门

- `slice_induced_correction_count=0`
- `infra_induced_correction_count=1`
- `scope_induced_correction_count=1`
- `verification_induced_correction_count=9`
- `verification:first_repo_contract_run_found_the_updated_permanent_negative_harness_content_pin_then_the_exact_new_hash_was_synchronized`
- `verification:second_repo_contract_run_found_the_C07C_card_label_and_frozen_P3_C07C_anchor_differed_then_the_card_was_made_explicit`
- `verification:full_local_Go_gate_found_the_pre_C07A_P3_C02D_isolated_schema_has_no_customer_merge_lineage_relation_and_failed_with_SQLSTATE_42P01`
- `verification:initial_clean_replay_wrapper_used_the_reserved_zsh_status_parameter_after_the_expected_cherry_pick_conflicts_then_conflict_resolution_continued_from_the_preserved_snapshot`
- `infra:dedicated_c07_slot_55432_aicrm_test_database_had_no_base_migrations_then_was_recreated_and_migrated_00001_through_00008_before_the_identical_acceptance_command_passed`
- `verification:first_post_S01_replay_contract_wrapper_had_an_invalid_JavaScript_object_token_before_shell_execution_then_the_same_read_only_gate_was_immediately_rerun_successfully`
- `verification:first_post_S01_full_ci_go_reached_the_final_query_plan_step_then_rejected_a_manually_mistyped_nonexistent_candidate_SHA_and_the_final_rerun_was_bound_to_git_rev_parse_HEAD`
- `scope:first_infra_count_receipt_patch_was_not_slice_anchored_and_transiently_changed_an_unrelated_historical_ledger_entry_then_was_reverted_before_commit_without_touching_business_files`
- `verification:first_verification_count_patch_matched_an_earlier_ledger_count_and_left_the_C07B1_block_stale_then_final_receipt_inspection_anchored_the_C07B1_count`
- `verification:first_final_receipt_batch_expected_the_stale_card_count_in_the_ledger_and_failed_atomically_then_the_exact_C07B1_block_was_inspected_and_corrected`
- `verification:first_post_S02_repo_contract_run_exposed_the_new_S02_ledger_content_pin_then_the_exact_union_hash_was_synchronized`
- `REAL_WECOM_NOT_EXECUTED`
- `PRODUCTION_DATABASE_NOT_EXECUTED`
- `LIVE_MIGRATION_NOT_EXECUTED`
- `C07B2_EXPLAIN_AND_PERFORMANCE_NOT_EXECUTED`

## 已解除的 precise defect receipt

- 原 status：`BLOCKED_OUTSIDE_FROZEN_WHITELIST`；当前 status：
  `RESOLVED_BY_P3_C07B0_PR_144_AND_POST_S02_LOCAL_FULL_GATES_PASS`。
- failing gate：完整本地 `make ci-go` 的 `acceptance/p3c02d`。
- exact failure：`TestCustomerEventsUseStableKeysetAndOwnerScope` 运行新生成查询时
  返回 `relation "customer_merge_lineage" does not exist (SQLSTATE 42P01)`。
- cause：`acceptance/p3c02d/customer_event_integration_test.go:createTables` 只在
  `acceptance_fixtures` 创建 `customers` 与 `customer_events`，且把 UoW
  `search_path` 固定到该 schema；C07A 合并后查询依赖的
  `customer_merge_lineage` 未被前移到该共享验收夹具。
- required expansion：当时修复至少需修改 `acceptance/p3c02d` 的共享测试 schema/DDL
  与它的 EXPLAIN query rewrite，或重构其数据库装配以使用 C07A
  migrated schema。这同时超出本片白名单并触及明确排除的 DDL / C07B2
  EXPLAIN 边界。
- resolution：C07B0 / PR #144 在独立白名单中前移共享 fixture 与 EXPLAIN
  rewrite，并以 squash commit `1ffeaba45c72a0e53a8763788cda1992ff253d24`
  合入 main；C07B1 未吸收该 DDL / EXPLAIN 改动。
- replay evidence：新 main 与 C07B1 叠加后的 `acceptance/contact`、
  `acceptance/p3c02d`、聚焦 race、sqlc 双生成、repo-contract 正负例与完整
  本地门均通过；最终 exact candidate receipt 见 ledger。
