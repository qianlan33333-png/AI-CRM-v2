# P3-S05B-R2: Segment CRUD 与成员预览运行链

## 输入合同

- Base SHA: `eb5713a8122d53e725dfc1c3de06b9eb16e2ad96`；application/go、application/web、repo-contract 与 secret-scan 均为 SUCCESS。
- OpenAPI 与 Segment public port 已冻结。本片只实现 list/create/update/member-preview 已声明 operation；不实现 GET detail。
- 00018 连续于已合并的 I5 migration 00017。旧 S5B 候选为只读证据，不移植其提交或修正历史。

## 可观察行为

- admin/ops 在固定全局 scope 内分页列出 Segment、创建或更新冻结 DSL 定义，并以 keyset cursor 预览物化成员。
- create/update 在同一 PostgreSQL UoW 中预留 Segment-owned receipt、写 Segment 与 event_log、完成 receipt；同 key 与规范化 payload 重放原结果，不同 payload 稳定冲突。
- 00018 的 deferred receipt guard 固定 `search_path = pg_catalog`，只读取 `public.segment_operation_receipts`；`pg_temp` 同名表不能绕过未完成 receipt 的提交拒绝。

## 边界与证据

- 不改 OpenAPI、public port、DSL/compiler、River、UI、其他域写权限或根依赖。
- 业务手写范围：application、HTTP、store、migration 与 API runtime mount；测试、sqlc 生成物、card/ledger/hash receipt 不计入业务规模。
- 本地证据使用 PG16.14 Segment slot 55433：clean 00001→00018、00018 down/up、receipt replay/conflict/rollback、catalog ownership 与 pg_temp shadow 永久负例。
- 生产数据库、live migration/cutover、真实企微与真实外发均为 `NOT EXECUTED`；三份真实 Audience 定义对拍为 `PENDING_EXTERNAL_GATE`。
