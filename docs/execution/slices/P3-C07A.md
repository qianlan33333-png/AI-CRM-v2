# P3-C07A：Contact transaction-bound 建档与 pairwise merge lineage

## 输入合同

- Base SHA：`42e2070422daef03d979de110278ffa49939e2c0`
- Phase：`P3 / contact pre-identity merge foundation`
- dependency：P3-I00 已合并，精确 main 三条 CI 全绿。
- supersedes：历史候选 P3-C07（`a39d783a429395177b29987d8f14a649d1a2eff9`）的
  A 边界；原候选只读，禁止 cherry-pick、amend、reset、继续开发或原样发布。
- 本片独立 correction 计数从 0 开始；历史 P3-C07 的 `slice_induced=4` 保留且
  `SUPERSEDED_BY_RESCOPE`，不得清零或改归因。

## 唯一可观察行为

Identity 的同一 transaction-bound UoW callback 可以调用 Contact：

1. `CreateForIdentity` 创建不含外部 identity 的渠道中立 customer；空姓名合法，
   owner/channel 仅接受正 ID 或 NULL。
2. `MergeCustomers` 按 customer ID 升序锁定恰好两个客户；保留调用方明确选择的
   primary，merged 软删除，标签取并集，追加 contact-owned lineage。
3. 同方向重放返回成功，包括原 primary 后续又被并入最终 root 的情形；反向、
   self、缺失、成环或无法证明同向 lineage 的请求 fail-closed。
4. `40001` / `40P01` 保留可由 UoW 识别的 PostgreSQL cause，同时对外错误文本
   只暴露稳定 sentinel；UoW 必须从 callback 起点整体重试。

所有写入只使用 caller transaction context；本片禁止创建 nested UoW，禁止写
`identities`、`customer_merges`、`pending_events`、`event_log`。

## 数据边界

- `00007_contact_merge_lineage.sql` 只创建 `customer_merge_lineage` 及其 primary
  查询索引；owner 为 contact。
- 本片不创建 `customer_event_idempotency`，不修改 lineage-aware timeline 查询，
  不实现 `AppendExternalEvent`。前者属于 P3-C07C，timeline 与真实生成 SQL 的
  PostgreSQL 16.14 EXPLAIN 属于 P3-C07B。
- migration 独立复核在 builder PR-ready 后由根任务恢复独立门禁执行；builder
  不自审、不 merge。

## 验收

- sqlc 连续生成两次无 diff，生成 manifest 同步。
- PostgreSQL 16.14 Goose up/down/up；迁移只含 lineage 对象。
- 真实 UoW 验证 create commit/rollback、tag union、soft delete、lineage、同向
  replay、反向/self/missing 拒绝及 downstream failure 整体回滚。
- 真实 `40001` 故障注入证明完整 callback 恰好重跑且只提交一条 lineage。
- focused race、全量 Go/Web、ownership/source policy、repo-contract permanent
  negatives、gitleaks/sensitive-path gates 全绿。

## 手写规模
- 实际为 13 files / 691 changed lines（675 additions，16 deletions），低于 15 files / 1500 lines 硬顶。
- 超过 12 files 软上限的唯一原因是同片必须完成历史 P3-C07 supersede ledger、治理漂移 backlog 与中央契约闭环；业务边界未扩大。

## 回滚与外部门

- 回滚为反向 migration 与回退本片 commit；不执行 live migration。
- `REAL_WECOM_NOT_EXECUTED`
- `PRODUCTION_DATABASE_NOT_EXECUTED`
- `LIVE_MIGRATION_NOT_EXECUTED`
