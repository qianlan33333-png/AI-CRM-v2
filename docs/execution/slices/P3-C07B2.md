# P3-C07B2：Contact lineage timeline generic plan 证明

## 输入合同

- Base SHA：`5651878273299ccfdae91f84c5d61476c8ab04e1`
- Phase：`P3 / contact lineage timeline query-plan proof`
- dependency：P3-C07B1 accepted head
  `640449af22bb154d0876d4215aa2ad0249cf3276` 已 squash merge 为 exact main
  `5651878273299ccfdae91f84c5d61476c8ab04e1`，exact-main application/go、
  repo-contract、secret-scan 全部成功。
- decision anchor：`P3-C07B2-2026-08-13`
- 本片独立修正计数从 0 开始；原 P3-C07B `slice_induced=3` 与
  P3-C07B1 `slice=0/infra=1/scope=1/verification=9` 历史不改写。

## 唯一可观察行为

对 P3-C07B1 实际 sqlc 生成的 `ListCustomerEvents` SQL 执行 PostgreSQL
16.14 generic prepared plan `EXPLAIN (ANALYZE, BUFFERS, FORMAT JSON)`，并永久证明：

1. `plan_cache_mode=force_generic_plan`，`pg_prepared_statements` 只记录 generic
   plan，不允许 custom plan 伪装。
2. 数据量至少为 20,001 customers、10,025 lineage edges、204,600
   partitioned timeline events，含 root + 50 descendants 与分布合理的干扰谱系；
   装载后对三张表显式 `ANALYZE`。
3. root 访问使用 `customers_pkey`，递归 lineage 访问使用
   `idx_customer_merge_lineage_primary`，timeline 访问使用真实月分区的
   `(customer_id, occurred_at DESC, id DESC)` 索引。
4. `customers`、`customer_merge_lineage`、`customer_events` 父表或任一
   `customer_events_*` 分区出现 `Seq Scan` 立即失败。

## 白名单与排除

- 只修改 C07B2 真实 PG plan acceptance、其 Make/CI 接线、永久
  repo-contract 正负门、本卡与 ledger。
- 只使用 C07A/C07B1 已合并 schema 与 SQL；不修改 query、repository、
  service、handler 或字段语义。
- 无 DDL、migration 或索引修改；禁止 `enable_seqscan=off`、手工
  等价 SQL、空表/小表假证明。
- 排除 P3-C07C、Identity、Segment、UI/OpenAPI、C07A merge 语义、
  C07B1 行为重写与任何性能调优。

## 验收

- c07 专属 PG16.14 slot 55432 真实 plan acceptance 通过。
- 去掉 generic-only、数据规模、`ANALYZE`、实际生成 SQL、lineage
  索引、timeline 分区索引或 no-Seq-Scan 任一证明时，永久负门失败。
- repo-contract 正例与完整负例、必要完整 `make ci-go`、两阶段
  自审通过。

## 回滚与外部门

- 本片无生产代码或 DDL，回滚为回退 acceptance / gate / governance
  commit。
- `REAL_WECOM_NOT_EXECUTED`
- `PRODUCTION_DATABASE_NOT_EXECUTED`
- `LIVE_MIGRATION_NOT_EXECUTED`
- `OUTBOUND_EXTERNAL_EFFECT_NOT_EXECUTED`
