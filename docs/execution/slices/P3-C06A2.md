# P3-C06A2：Contact S 档性能验收器与收据合同

## 输入合同

- Base SHA：`47fbaebce120ef9f4f128f94588d396abb128da3`
- Phase：`P3 / contact wave`
- slice_kind：`performance_acceptance_runner`
- 决策锚点：`USER-CONTINUE-THROUGH-P4-2026-08-12`
- 依赖：P3-C06A1 已合并且精确 main CI 全绿。

## 完整行为

1. 使用真实 `CustomerQueryRepository`、transaction-bound UoW 和生产 sqlc 查询，
   覆盖 5 个筛选器的全部组合、删除态、两个时间窗各四种边界、首页/真实 keyset
   第二页及 50/200 limit，共恰好 4096 个场景。
2. 每场景至少 warmup 3 次、测量完整 UoW 调用至少 20 次；首页和第二页必须均
   满页、`has_more=true`，第二页不得与首页重叠。逐场景及全局 P95 必须 `<200ms`。
3. pgx tracer 必须捕获每次真实 repository 的两条冻结 SQL，并以原 SQL 和参数
   执行 `EXPLAIN (ANALYZE, BUFFERS, FORMAT JSON)`；`customers` 或
   `customer_tags` 出现 Seq Scan 即失败。
4. 运行器只接受私有绝对路径 `--database-url-file`，DSN 仅允许字面
   `127.0.0.1`/`[::1]`、精确数据库 `aicrm_perf` 和 `sslmode=disable`；不得从
   argv 接受凭据。收据校验必须同时接收调用方提供的可信 main SHA，不允许只靠
   收据内部 SHA 自洽。
5. 收据严格记录并复核 S 档环境、200,000 客户/600,000 标签关系、六个时间分布
   区间、热点分布、4096 场景、样本、P50/P95/max 与完整原始 EXPLAIN JSON；
   解析器必须重新遍历原始计划并核对摘要。unknown field、trailing JSON、symlink、
   负 buffer、缺场景、非预期 SHA 或非预期 main CI URL 均 fail-closed。

## 精确构建与 C06B 边界

- A2 本片只交付机制，不连接服务器，不宣称性能通过。
- C06B 必须从 A2 squash merge 后、main CI 已全绿的干净 checkout 执行
  `go build -trimpath -o <path> ./cmd/aicrm-contact-perf`。不得使用仓库通用
  `build_release_binary.sh`，因为该脚本的 `-buildvcs=false` 会有意移除本验收器
  必须读取的 `vcs.revision`/`vcs.modified` 元数据。
- 运行与离线复核均显式传入同一个 A2 main merge SHA；收据再绑定 main CI URL。

## 安全与数据边界

- 只连接用户授权测试服务器上隔离的 `aicrm_perf`；不连接 5432 本机现有实例、
  staging/生产库或 `150.158.82.186` 旧服务器。
- 不修改 S 档参数、不升配；不执行真实企微调用、生产部署或 live migration。
- 收据标记为 `authorized_test_server_synthetic`，不得冒充生产或真实用户数据。

## 验收

- focused race/vet、source-policy 及复制路径负例、repo-contract 永久负例。
- 连续生成无 diff、全量 Go/Web/repo-contract/secret 门、中文 PR、squash merge、
  精确 merge SHA main CI。
- P3-C06B 才创建/迁移隔离性能库、seed、运行 4096 场景并提交真实性能收据。

## 规模与回滚

- 验收器、可信收据和两项精确中央门禁是一个完整 CLI 行为，实测 5 文件 / 1030
  手写变更行；因完整 EXPLAIN 存证使实现略超默认 1000 行，按完整行为优先使用
  硬顶额度，仍远低于 15 / 1500。测试与治理不计。
- 回滚为回退 A2 squash merge；本片没有服务器状态可回滚。
