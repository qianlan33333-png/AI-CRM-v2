# G1-D02：规则签字固化与 P2 放行

## 输入合同

- Base SHA：`57bb4ca4b4b8e1b46978e6e513f6d9cdf28f3af7`，与执行时 `origin/main` 一致。
- 决策锚点：`G1-D02-2026-08-10`。
- 执行者：Codex-Sol；中央合同区未委派。
- 输入：781 条 route triage/API mapping、293 条 feature matrix、316 条 migration mapping，以及用户确认的迁移例外和 top-20 清单。

## 行为边界

1. 按实际 tier 签字：A 501 `MIGRATE`、B 268 `DEFERRED_POST_LAUNCH`、C 12 继续 G1-D01 `NOT_MIGRATED`。
2. 293 条 feature 只做治理签字；不伪造实施、浏览器或生产验证。
3. 316 条 migration 按批准规则转终态；`ABSENT_AT_HEAD` 只能 fail-closed 为 `DEFER`，不写成已证实脏数据。
4. 不生成迁移代码，不连接真实旧库，不调用企微，不部署。

## 验收

- `make feature-matrix-p1-completion`：293 行、pending 0。
- `make migration-mapping-p1-completion`：316 行、pending 0。
- `make p1-reconciliation-contract`：781 路由、316 表、pending 0。
- `make ci-go`、Web、repo-contract、secret scan、PR CI 和精确 merge SHA main CI 必须通过。

## 收据与修正

- 独立机械复核确认：95/98/33 集合计数成立；33 个 MIGRATE 候选中 24 个可确认具体语义转换，9 个仅能确认目标合同未冻结。
- 切片修正数：`1`（准备阶段文档路径归位）；基础设施修正数：`0`。
- PR/head/merge/main CI 由后续切片按 ledger 的 forward-recorded 语义回填。

## 外部门

M0-4 分支保护继续作为非阻塞外部门。真实企微、生产数据库、staging/生产部署与 live migration 仍是 `PENDING_EXTERNAL_GATE`。
