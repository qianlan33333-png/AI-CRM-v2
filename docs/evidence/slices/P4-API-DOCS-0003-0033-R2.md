# P4-API-DOCS-0003-0033-R2 evidence

## Candidate receipt

- exact-main base: `d81ba86bde4752fe6d3e402f7bf5bebf007db600`
- base tree: `54397c51b4faebbc8de806b9b3dbe050dcae2490`
- base override decision: `P4-API-DOCS-0003-0033-R2-BASE-OVERRIDE-2026-08-18`
- Pro conversation: `https://chatgpt.com/c/6a8408b9-b520-83ea-bb1b-7e4bd626454a`
- facts appendix: `docs/evidence/p1/P4-API-DOCS-MCP-R2-FACTS.md`（staged bundle gitleaks PASS）
- candidate status: untrusted input until clean replay and local/remote gates complete.

## Implementation notes

- 开发在 exact base 的 detached clean worktree 中完成；含用户 P2 WIP 的历史主 root 未被使用。
- 变更严格限制在指挥白名单 14 项内；无 migration、provider、worker、tenant、OneID 或外部效果路径。
- `go.mod` 仅将 `gopkg.in/yaml.v3 v3.0.1` 从 indirect 改为 direct；`go.sum` 零 diff。
- canonical OpenAPI 变更通过 canonical generation 链路验证；generated 输出未手改。
- 页面 JS 搜索最初基于卡片 summary 的 textContent（会带入认证徽标文本），实现自查改为
  仅 method+path+summary 的 `data-search-text` 属性，与冻结合同一致。计为 slice-induced 修正 #1。
- `openapi-p1-contract` 门禁指出两处声明问题：flow mapping 内未加引号的 500 description
  被逗号截断成幽灵键；两个 operation 缺少 `x-aicrm-data-classification: internal`。
  修复仅限本片的两个 operation 块；orval 全量生成物 `web/src/api/generated/health.ts`
  随之由 canonical generation 更新（+127 行），未手改。计为 verification-induced 修正 #1。
- 指挥第一轮复核（CHANGES_REQUIRED，SCOPE_FROZEN_REPAIR_ONLY）后修复：
  - Feature Matrix LEGACY-S05-059 由 IMPLEMENTED 回退为 IN_PROGRESS（SYNTHETIC_PASS 保留，
    证据列只写 candidate/路径/status=local_candidate_not_merged，不含虚构 PR URL 或 merge
    SHA）；IMPLEMENTED 声明待 R2 squash merge 后由后续真实业务 PR（0757 新切片）登记。
    计为 slice-induced 修正 #2。
  - `api.OpenAPISpec()` 改为 `bytes.Clone` 返回副本，不再暴露包级嵌入切片，并补永久负例
    （改废第一次返回值，第二次读取仍与 canonical 一致）。计为 slice-induced 修正 #3。
  - 页面 ServeHTTP 改为先完整渲染进 buffer 再写 200；模板失败时 canonical 500 不再可能
    伴随部分 HTML 写出，并补「无部分 HTML」断言。计为 slice-induced 修正 #4。
  - 方法边界测试扩展为 HEAD/OPTIONS/POST/PUT/PATCH/DELETE 六方法表驱动，对两条路由断言
    405、`Allow` 精确为 GET、认证/授权/CSRF 零调用、响应不含端点元数据；401/403 增
    canonical JSON envelope 断言。行为本就正确，仅新增断言，计为 verification-induced 修正 #2。
  - mapping 0003/0033 的 disposition_reason 改为保留 G1-D02 原文的追加式说明并标注
    status=local_candidate_not_merged；decision_evidence 数组按裁决保持原样。
- `LEGACY-API-0034` 保持 `DEFERRED_POST_LAUNCH`：路由未注册、mapping disposition 未改、
  静态测试 guard 锁定。

## Local verification

- focused builder/handler/security 测试（含 race）: PASS
- 注册级黑盒（401/403/405/302/404，认证前 405）: PASS
- `go test -race -count=1 -timeout=240s ./cmd/aicrm`: PASS
- headless 浏览器冒烟（`cmd/aicrm/testdata/api_docs_browser_smoke.mjs`，115 个端点）: PASS
- `go test ./...`、selected go gates、api codegen 检查、repo-contract、secret scan: 见复核产物
  test-report（本文件写入时上述全量门禁按序执行中，结果以最终复核包为准）

## External boundary

- historical/production database: NOT_EXECUTED
- staging/production deployment: NOT_EXECUTED
- provider, URL fetch, blob read, real WeCom/payment/outbound: NOT_EXECUTED
- status: INTEGRATING, not MERGED or DEPLOYED

## Exact-main closure addendum

- reviewed head: `c1370ef1115e88f5ac9fb82c8aa808283402d671`
- protected PR: `#248` (`https://github.com/qianlan33333-png/AI-CRM-v2/pull/248`)
- sole required check: `ci / merge-gate` PASS in run `32125106786`
- match-head squash merge: `98ab41477cfa266114c170031d6c42c9c83ba980`
- reviewed-head tree and merge tree: byte-identical
- current status: exact-main CLOSED, not DEPLOYED
- staging/production/provider/real outbound effects: NOT_EXECUTED
