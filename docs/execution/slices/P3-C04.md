# P3-C04：Contact 客户列表 UI

## 输入合同

- Base SHA：`f1c2fa84595fb66bb42e7333c163035cd997689a`
- Phase：`P3 / contact wave`
- slice_kind：`delegated_ui_after_openapi_and_orval_freeze`
- 决策锚点：`USER-CONTINUE-THROUGH-P4-2026-08-12`

## 完整行为

1. `/customers` 路由使用已生成的 Orval `listCustomers` client 读取真实列表，
   不手写 HTTP 路径、不解析或伪造 opaque cursor，请求固定
   `credentials: same-origin`。
2. 支持关键词、负责人、阶段、渠道、标签、删除状态、加入时间、
   最近互动时间与分页数量筛选；输入不合法时 fail-closed，不发请。
3. 严格校验响应 envelope、Customer 字段、watermark、估算总数、单页与跨页
   重复 OneID；继续分页出现 watermark 变更或非法数据时保留已加载列表并
   显式报错。Avatar 边界只接受无凭据的绝对 HTTP/HTTPS URL。
4. 加载、空结果、401/403/400/服务不可用、重试与加载更多均有稳定、
   可访问的 UI 状态；401 回调父 shell 失效 session，数据范围始终以服务端为准。

## 非范围

- 不实现客户详情、写操作、标签/阶段变更、时间线或客户列表后端。
- 不修改 OpenAPI、公共 port、migration 或根依赖；不连接生产数据库，不部署。
- 20 万数据与 2C4G P95 硬门由 P3-C06 独立验收。

## 验收

- Focused Vitest 覆盖筛选、参数映射、严格响应解析、加载/错误/空状态、
  分页完整性、角色提示与 shell 路由接入。
- Orval 连续生成无 diff；固定 Node 24.18.0/npm 11.12.1 的全量 Web CI，
  全量 Go/repo-contract/secret scan。
- 中文 PR、squash merge 和精确 merge SHA 的 main CI。

## 规模例外

Terra 收紧后的 UI 手写源为 999 行；Sol 路由接入、安全 URL 边界和永久门禁
使本片略超 P3 默认 1000 行上限，但仍低于 1500 行硬顶。这些都是客户列表
真实路由闭环与防手改门禁的必要部分，拆分会留下不可观察的半成品。

## 回滚

回滚本片 merge；本片无 DDL、持久化写入或外部效果。

## correction

- `slice_induced=3`：Terra 首轮将异常运行时 draft 处理为抛错，已改为 fail-closed；
  Sol 首次复核又发现响应字段和跨页 watermark/重复 OneID 未严格验证，
  已补齐并在失败时保留旧列表。Sol 集成复核时将 avatar URI 从任意 scheme
  收紧为无凭据绝对 HTTP/HTTPS URL，防止未来渲染链引入可执行 scheme。
- `infra_induced=1`：Terra 默认工具链命中 npm 11.16 且无依赖目录，已使用固定
  npm 11.12.1 并执行 `npm ci`。
- `scope_induced=1`：首版手写源约 1180 行，Terra 等价收紧为 999 行；Sol 后续
  必要接入依「规模例外」闭环，未超硬顶。
- `verification_induced=1`：ESLint 将类型回调参数误判为未使用，改为兼容的事件
  handler 类型写法，未改变业务行为。
