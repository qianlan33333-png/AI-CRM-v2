# P0-C04 acceptance evidence

状态：`LOCAL_CONTRACT_COMPLETE`

证据层级：`LOCAL CONTRACT`。P0-C04 由内部 `gpt-5.6-terra`（`ultra`）task
`/root/p0_c04_finalize` 在独立 worktree 执行；`root` 冻结架构、独立验收并保留
Git/GitHub 权限。P0-S02 只会由内部 Terra 在派发后的独立 worktree 实施。本合同
不包含 P0-S02 实现、真实 HTTP listener、staging、production、数据库或企微验证。

## 精确基线与范围

- base：`main@c5ad1f49b66f5a8dd5d3727f3b053e31205f3b63`
- branch：`codex/p0-c04-p0s02-contract`
- P0-S01 squash merge：`eb998ebe78de6d0521009e57d9f4b184026b0158`
- P0-S02 实现白名单：`internal/platform/http/health.go`、
  `internal/platform/http/health_test.go`
- P0-S02 尚未派发：task ID、worktree、完整文件 manifest SHA 与实现 diff SHA
  均不能预填；root 在 Terra 返回 manifest、stage 后才计算未来实现的 canonical
  `git diff --cached | shasum -a 256`。
- root 复验后记录 payload diff SHA `7fed276b…6619` 与 payload manifest SHA
  `2e365163…b40c`；ledger/evidence 两个 receipt carrier 由 PR head/merge SHA 覆盖。
- 首轮中央验证发现 tagged acceptance 导入尚不存在的同模块 package，会让
  `go mod tidy` 尝试解析远端模块并失败。root 增加无实现的 package 锚点
  `internal/platform/http/contract.go`；该失败不计为 P0-S02 业务实现证据。

## 冻结合同

- 只实现生成的 `generated.StrictServerInterface`，返回生成的 200 JSON 类型与
  `generated.Ok`，禁止手写 JSON/状态码。
- `/healthz` 仅为 process liveness，不查询 DB、River、settings/env、网络、
  时间、主机或 readiness。
- 中央 tagged acceptance 通过 strict adapter 与生成 router 断言精确 HTTP
  response；静态脚本可在无 `.git` 输入包运行。
- P0-S02 尚未实现时，`make ci-go` 必须显示 completion gate `PENDING` 而保持
  基础 CI 绿色；任一白名单实现文件出现时，缺文件或合同不合格必须硬失败。

## 本地复验（LOCAL CONTRACT）

| 项目 | 命令/断言 | 退出码与结果 |
| --- | --- | --- |
| 无实现基础门 | `make ci-go`；最终以独立任务 `TMPDIR` 重跑 | `0`；所有基础门 PASS，输出 `P0-S02 completion gate: PENDING (implementation not present)` |
| 无 Git 正例 | 临时普通目录中 `GOFLAGS=-mod=readonly acceptance/p0s02/static_contract.sh` | `0`；package race test 与 static PASS |
| 无 Git 正例 | 同目录 `GOFLAGS=-mod=readonly go test -race -timeout=15s -tags=p0s02_acceptance ./acceptance/p0s02` | `0`；生成 strict adapter/chi 精确断言 PASS |
| 无 Git 正例 | 同目录 `GOFLAGS=-mod=readonly make mod-check` 与 `test ! -e .git` | 均为 `0`；fixture 未含 Git 元数据 |
| 缺文件负例 | 删除 `health_test.go` 后运行 static | `1`；报告缺少 required Slice file |
| 手写 transport 负例 | 添加 `net/http` 后运行 static | `1`；报告 forbidden dependency |
| DB 副作用负例 | 添加 `database/sql` 后运行 static | `1`；报告 forbidden dependency |
| 非 `generated.Ok` 负例 | 将 response status 改为 `not-ok` 后运行 static 和 tagged acceptance | 均为 `1`；strict HTTP body 也被拒绝 |
| repo 合同 | `scripts/check_repo_contract.sh`；`scripts/test_repo_contract.sh` | 均为 `0`；永久 repo-contract 负例 PASS |
| 生成/无 Git | `make generate-check`；`make gitless-generate-test`；`make mod-check` | 均为 `0` |
| Secret | `gitleaks version`=`8.30.1`；`gitleaks git .`；对 staged tree 的 `gitleaks detect --no-git`；`scripts/scan_sensitive_paths.sh` | 均为 `0`，无泄漏 |
| 格式/差异 | Ruby YAML load、`bash -n`、`gofmt -l`、cached/working `git diff --check`、unstaged diff | 均为 `0` |
| 协作治理 | P0-S02 legacy external-term scan；派发字段与 ledger null-state 复核 | `0`；无伪造 task/worktree/diff/manifest SHA |


静态脚本首轮在 macOS 将 `rg -E` 误解为编码参数，合格 fixture 被错误拒绝；
已最小改为默认正则模式 `rg -q`，随后上述无 Git 正例重新通过。
既有 P0-S01 gitless process fixture 抖动已由前置 P0-C04F（PR #10、
`main@c5ad1f49b66f5a8dd5d3727f3b053e31205f3b63`）修复并通过 main 三门；
本片重放后继续完整复验，不沿用旧的偶发 PASS。

## 未执行项与边界

- P0-S02 业务实现：`NOT_IMPLEMENTED`；本仓库未新增 `health.go` 或
  `health_test.go`。
- PR、远端 CI、squash merge、main 三门：`PENDING_EXTERNAL_GATE`。
- 真实 listener、真实 HTTP 服务、数据库、River、staging、production、企微：
  `NOT_AUTHORIZED` / `NOT_EXECUTED`。本证据中的 HTTP 仅为本地 `httptest`。
