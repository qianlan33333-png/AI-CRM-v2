# P3-O6B2：失败/已取消外发人工重试

## 输入与目标

- Base SHA：`befdba33e229ad09a378df043ea9dea859d7584e`；O6B1 PR #198 已 CLOSED，exact-main 三项 required CI 全绿且 open PR 为 0。
- 只交付 `final_failed/cancelled → pending` 的幂等人工重试；同一 task 在同一失败代际最多产生一个 control result、一个新 generation link 与一个 River job。
- 连续 migration 为 `00024`，仅扩展 O6B1 `outbound_control_receipts` 的 `manual_retry` 不可变结果约束，不新增表。

## 冻结行为与边界

- 只允许 `final_failed` 与 `cancelled`；`retryable_failed/outcome_unknown/sent/sending/pending` 稳定返回 transition conflict。`outcome_unknown` 绝不盲目重试，O6A 仍是 `retryable_failed` 的唯一自动重试机制。
- task 行锁冻结源状态与最新 generation；两个不同 idempotency key 竞争时，首个提交后 task 已为 `pending`，后者冲突。相同 key 丢响应重放返回原 receipt/event/job。
- 复用 O6B1 `OutboundControlClient`，只增加两个冻结 typed args（single/batch）的 River 官方 `InsertTx`；无万能 port、无动态 SQL、Outbound SQLC 不读写 `river_job`。
- 新 River job、generation link、`outbound.manual_retry_requested` event、`pending` task 投影与 completed control receipt 同一 UoW；任一步失败全部回滚，本命令不调用 provider。
- 不含 HTTP/API/O7、O8/UI/OpenAPI、新产品能力、生产 DB、live migration、真实企微或真实外发。

## 黑盒验收

- C07/55432 PostgreSQL 16.14：双连接同 key 重放稳定；双连接不同 key 竞争同一失败代际时仅一个 control result、generation 2 与 River job；`cancelled` batch 完整解码 typed `batch_id/chunk_index/task_id`。
- 全部拒绝状态、异 payload 冲突、event failure 的 task/receipt/link/job 回滚均 PASS。
- 带完成人工重试数据执行 `24→23→24` 事实不丢；O6B1 `23→22→24` 与 O6A/O5 `22→21→24` 历史兼容 PASS。
- 独立 Agent 不读取 migration 源码，以 read-only transaction 对最终水位 24 做 catalog + data 对账 PASS：18 个约束全部 validated、5 个索引全部 valid/ready/live、无 River FK；最新 manual-retry receipt/task/event/generation 1→2/new River job 及 single typed args 全部一致。

## Route / matrix

- O6B2 不新增 HTTP route，不把 legacy 重试路由或 `LEGACY-S06-026` 虚报为已实现。
- O7 将调用本片真实 service，并在具有 route/PR/SHA/黑盒证据后精确维护 route/matrix。

## 修正归因与外部门

- `slice_induced=1`，`infra_induced=0`，`scope_induced=0`：两阶段自审发现首版只证明相同 idempotency key 的并发重放，随即补充不同 key 竞争同一失败代际的双连接永久验收；业务实现无需修改。
- `verification_induced=5`：首次调用全仓 generate 时本新 worktree 尚无 pinned Orval，改用仓库固定 SQLC target 定向生成；首轮 PG 断言误用 River catalog 列名 `encoded_args`，只读 catalog 确认 0.24 schema 为 `args` 后修正黑盒读取；首次本地 `make ci-go` 未带 workflow 明示的 `P0S03_PG_INTEGRATION=1` 与 C07 DSN，在防误跑门按设计失败，形成单一候选 commit 后以 workflow 同构环境和精确 parent/head 重跑；PR 首轮 Ubuntu repo-contract 暴露 GNU `grep -q` 提前退出与 `pipefail` 组合造成的 Makefile `printf` SIGPIPE，解析改为等价 here-string；补记该验证收据时先在 amend 前运行 append-only ledger 门被识别为历史漂移，随后按候选 commit 原子 amend 并从 main 父 SHA 复验。五项均未修改业务实现。
- 外部门：`REAL_WECOM_NOT_EXECUTED`、`OUTBOUND_EXTERNAL_EFFECT_NOT_EXECUTED`、`PRODUCTION_DATABASE_NOT_EXECUTED`、`LIVE_MIGRATION_NOT_EXECUTED`、`DEPLOYMENT_NOT_EXECUTED`。
