# P0-S04：River v0.24 adapter

状态始终为 `BLOCKED_ON_P0-C06B_GATES`；C06A 合并也不表示 P0-S04 ready 或 complete。

- `execution_mode: internal_terra`
- C06A task：`/root/p0_c06a_river_contract_replay`；model：`gpt-5.6-terra`；reasoning：`ultra`。
- worktree：`/Users/qianlan/Downloads/新CRM/handoffs/terra-worktrees/P0-C06A-R1`；branch：`codex/p0-c06a-river-contract`；base：`511bf61f4d553d1e5f6bd3f82edc00f6f8fdc9f4`。
- C06B dispatch base：`P0_C06A_MERGE_SHA_PENDING`；P0-S04 implementation base：`P0_C06B_MERGE_SHA_PENDING`。

C06A 只冻结 River root/`riverpgxv5` 的精确 `v0.24.0` 依赖、River-neutral typed contract 和 `p0s04_acceptance` tagged acceptance；不实现 adapter、静态门、shared CI 门或真实 PG。未来 adapter 仅可位于 `internal/platform/river/{migrate.go,runtime.go,runtime_test.go}`，不得接入 cmd/API、scheduler/job/timer，不得导入 River `internal/**`、手写 River DDL 或自动迁移。

公开目标形态是 `Runtime`、`NewRuntime(Lifecycle) *Runtime`、`(*Runtime).Run(context.Context) error`（满足 `runtime.Component`）和 `Migrate(context.Context, *pgxpool.Pool, Direction, *MigrateOptions) error`。Run 保留 Start/Stop 原错；cancel 后用 live、bounded context Stop；提前 Stopped 返回 `runtime.ErrUnexpectedStop`。Migrate 只接受 up/down，invalid 方向先返回 sentinel/text；最多透传 `TargetVersion`，不开放额外迁移选项。基础设施语义为 at-least-once。

tagged acceptance 冻结公开 River surface、6 migrations、上述 lifecycle 与 nil/non-nil pool invalid-direction 行为；未设置 `ALLOW_DESTRUCTIVE_RIVER_MIGRATION_TEST=1` 时必须是 `PENDING_EXTERNAL_GATE`，并且当前因 `Runtime`/`Migrate` 尚未实现而预期编译失败、不会连接 PG。C06B 才对真实 adapter 施加 AST/static/gitless/shared CI 和固定 loopback `aicrm_test`、PG `160014`、official up/Validate/down(`TargetVersion=-1`)/up 六表门。

`v0.24.0` peeled commit `3cfef1a63d8dc58b795d8a3958ba5d36cf02a217` 仅为已冻结的历史缓存证据；本次未联网核验。
