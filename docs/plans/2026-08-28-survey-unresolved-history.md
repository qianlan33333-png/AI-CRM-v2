# Survey 未解析历史最小落表

保持全量迁移，先于营销剩余24条。正式V2只读核查：10问卷/57题/189选项已导入；435提交/2275答案已导入；1144提交及其4327答案仍以 `survey_definition_history_unresolved` 隔离，共5471。归档没有缺失旧题目的定义表，不能按标题猜配当前题目。

仅补127的两张Survey-owned历史表，复用现有五表读取与提交组分区。保存全部旧提交及答案快照；源题目/选项ID明确为V1引用，不伪装成当前V2定义。原冻结quarantine回执不改，新history版本逐行回执证明补充落地。QuestionnaireID只允许既有核验导入回执解析，CustomerID只允许DM01映射；缺失关联保持NULL并报告。

三条只读入口：历史提交列表/详情/该提交答案分页。复用QuestionnairesRead、既有问卷页的历史入口；不写当前提交统计、结果、评分、标签、推送或队列。来源HMAC和身份/重定向/评测摘要不返回API；原加密归档保留。

先纯分区守恒测试及真实归档只读预检，再Store事务回滚与实际隔离导入/重放/双对账。合并遵循最终SHA CI与候选Full全绿，合并后exact-main全绿才部署；当前分支从e67c5b94创建，120–126尚待统一集成，不能直接部署。
