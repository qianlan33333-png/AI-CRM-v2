# P0-C05A：P0-S03 类型合同与 PG 验收层

状态：`CONTRACT_FROZEN — PENDING_P0-C05B_AND_P0-S03`

执行模式：`internal_terra`

证据层级：`LOCAL CONTRACT`。未触及预发、生产、真实数据、企微或迁移。

## 拆片与边界

- base：`main@dd44d32826a11ac5f20a68477c09fbc60ba6c994`；branch：`codex/p0-c05a-sqlc-typed-contract`
- task id：`/root/p0_c05a_integration`；executor：`gpt-5.6-terra`，`reasoning_effort=ultra`
- worktree：`/Users/qianlan/Downloads/新CRM/handoffs/terra-worktrees/P0-C05AR1`
- payload（下列 sorted `MODE BYTES SHA256 PATH`）：`acceptance/p0s03/query_contract_test.go`、`docs/execution/slices/P0-S03.md`、`internal/platform/store/contract.go`；receipt carriers `docs/evidence/slices/P0-C05A.md` 和 `docs/execution/slice-ledger.yml` 均排除。
- payload manifest SHA-256：`8880026348f7f8ea85513bbbbb1dd4d712a9f8433f05a5319113aed97fc9aa0a`；pre-stage 与 root staged canonical binary diff SHA-256 均为 `aef95e85d60b65422b62b72b98a5cf121a523528eab2c53c570241c9881d2206`。
- 历史拒收事实：旧 P0-C05 候选为 6 文件、708 行，超过 400 行硬门；其 6 路径 untracked unified diff 为 29,562 bytes、SHA-256 `f91ef664c096354ee45f778ef69e0104afe1f4a7ea0da9137862f0db6d35d0e8`。此条仅作拒收审计记录，不主张其为本片 payload 的代码来源。
- 本 task 相对 base 只限五个白名单路径；payload 三路径之外的两个 receipt carriers 不参与 manifest 或 diff hash。不实现 `PingStore`，不改查询、生成物、依赖、共享 CI 或 checker。

```text
100644 5086 8ec6608f0856f33d5d3e76f497a88a26d0c54ea65c23b4744a08ef7f9c1ff072 acceptance/p0s03/query_contract_test.go
100644 3186 7a3f4fb464179efb3ac9774ba2aaff469c8b058b493f911537c1e1e2ae1994c2 docs/execution/slices/P0-S03.md
100644 98 747683b0f430da2ee29f001abaebe5fe621561aa3dd99b5b9db6b7d871895165 internal/platform/store/contract.go
```

冻结行为为：未来 `ping.go`/`ping_test.go` 仅可在 220 行内、零依赖实现
`PingStore{querier dbgen.Querier}`；构造器只能 `dbgen.New(db)`；生成值 1 成功、
底层错误保持 `errors.Is`、其他值为精确 `platform store ping: unexpected value %d`。
集成只接受 `P0S03_PG_INTEGRATION=1` 和完全相等的固定 loopback `aicrm_test` DSN，
不读 `DATABASE_URL`，拒绝错误不回显输入；它验证 PG `160014` 与真正生成查询。

静态/gitless scanner 及共享 CI/Makefile 接线不属于本拆片，由依赖 `P0-C05B` 冻结。
P0-S03 的实现基线固定为 `P0_C05B_MERGE_SHA_PENDING`，须由 root 在两片合并后替换。

## 本地验证

| 类型 | 命令/情形 | 退出码 | 结果 |
|---|---|---:|---|
| LOCAL | `gofmt -d`；YAML 唯一性、`git diff --check`/白名单 | 0 | PASS。 |
| LOCAL | `make generate-check`；`make mod-check` | 0/0 | PASS；连续生成无 diff、无 module drift。 |
| LOCAL | `GOFLAGS=-mod=readonly go vet ./...`；`GOFLAGS=-mod=readonly go build -trimpath ./...` | 0/0 | PASS。 |
| LOCAL | `GOFLAGS=-mod=readonly go test -race ./...` | 0 | PASS；anchor 不破坏普通 race 基线。 |
| LOCAL | `make vuln` | 0 | PASS；reachable vulnerabilities 为 0。 |
| LOCAL | `make ci-go` | 0 | PASS；含 gitless、P0-S01，P0-S02 正确仍 pending。 |
| EXPECTED NEGATIVE | 清空 PG 开关后 `go test -tags=p0s03_acceptance ./acceptance/p0s03` | 1 | `PingStore`/构造器未定义，正确 build failed，未连 PG。 |
| SECURITY | root staged repo-contract/永久负例/custom scan；gitleaks history/tree | 0 | PASS。 |
| PG | 显式开关的 tagged PG16.14 验收 | `NOT EXECUTED` | P0-S03 尚未实现；未连接数据库。 |

本片未执行 Git、GitHub、部署、生产操作或数据库迁移。后续实现须追加真实命令、
stdout/stderr、退出码和证据层级，不得覆盖本冻结记录。
