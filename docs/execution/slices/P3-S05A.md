# P3-S05A：Segment 手动刷新接受收据

## 输入合同

- Base SHA：`d0c32885c9b063068696d9e52570f70e6f702c72`，其 application/go、repo-contract、secret-scan 均为 CLOSED success。
- 只复用已合的 S-4 `RefreshOnce` 与 River `heavy` 队列事实；不读取旧 Python，历史 P3-S05 候选为 `HARD_STOP_READ_ONLY`，仅保留其“receipt 必须登记 table ownership”的行为证据。
- 冻结 OpenAPI 已定义 `POST /api/v1/segments/{segment_id}/refresh`、`segments.write`、CSRF 与 Idempotency-Key。相同 caller key 复用不同规范化命令必须是 `409`，因此该冲突分支与 accepted 收据不可拆分。

## 可观察行为

- 合法已授权请求在单个 PostgreSQL UoW 内为 `(admin actor, Idempotency-Key)` 保留 Segment-owned 收据，插入显式 `heavy` 的 `segment_refresh_requested` River job，再将收据置为 `accepted`。
- 同一 actor、同一 key、同一 segment 重放只返回最初的 `{status:"accepted",segment_id}`，不会再插入 River job；同 key 指向不同 segment 返回 `409`。
- 收据预留、入队或接受任一步失败均回滚，不返回 accepted。该 response 只陈述命令已持久化入队，不陈述成员已经刷新。

## 硬边界

- 不改 OpenAPI、CRUD、UI、S-4 worker、`RefreshOnce`、scheduler、外部效果或根依赖。
- 新增 `segment_refresh_receipts` 的 migration、Segment ownership、sqlc repository、handler 与 PG16.14 acceptance 是该一个业务行为不可分的最小实现；不是纯 DDL 或治理变更。
- 生产数据库、live migration/cutover、真实企微、真实外发与三份真实 Audience 定义对拍均为 `NOT EXECUTED` / `PENDING_EXTERNAL_GATE`。
