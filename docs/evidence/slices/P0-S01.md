# P0-S01 acceptance evidence

状态：`PRO_IN_PROGRESS`

证据层级：当前只有 `LOCAL INPUT PROVENANCE`；尚无 Pro 输出、应用实现、PR、
merge、staging 或 production 证据。

## 输入 provenance

- base：`main@7ae62e2aed112d70341b6ea435bb08e7eac80de7`
- branch：`slice/p0-s01-go-role-startup`
- [Issue #5](https://github.com/qianlan33333-png/AI-CRM-v2/issues/5)
- [ChatGPT Pro 对话](https://chatgpt.com/c/6a77278d-24a4-83ee-9f01-515784ff49e5)
- ZIP：`P0-S01-source-7ae62e2aed11.zip`
- bytes：`65,763`
- SHA-256：`d318ce5b2082e33c6ce50501e490fbd3349033d9359875acf5f4e6619922a7af`
- gitleaks：`8.30.1`，全历史与 payload 均 PASS；payload 报告为 `[]`

包不进入仓库。它由 base 的 27 条白名单路径精确展开为 32 个 Git blobs，另加
`BUNDLE-MANIFEST.txt`；独立复验内容与 mode 全部一致。ZIP 共 53 entries（33
files、20 directories），无 `.git`、`.github`、中央负例脚本、依赖、缓存、
数据库、运行状态、symlink/special、路径穿越、敏感文件名或凭据模式。

## 发送前验证

以下均为真实本地结果：

- `zip -T`：PASS
- 外部 `SHA256SUMS`：ZIP 与 gitleaks report 均 PASS
- gitleaks 8.30.1 与自定义敏感路径/PEM/API key/Cookie 扫描：PASS
- 普通无 `.git` 解压目录 `make generate-check`：exit 0
- 普通无 `.git` 解压目录 `make mod-check`：exit 0
- 从外部 cwd 调用 static contract：按预期因实现未交付 exit 1，精确报告缺少
  `cmd/aicrm/main.go`，不再出现 Git 错误
- 门禁前后 payload 文件集合、内容与 mode：无变化

## 发送前纠错

旧包 `P0-S01-source-fd766b0b30ae.zip`（63,523 bytes，SHA-256
`095275c6c6908a72c981b03901ce060704655e741daaab2c0fe41983dbe4034a`）
安全扫描通过，但独立验包发现中央门禁依赖 Git。Codex 未把缺陷转交给 Pro，先以
P0-C03 修中央合同并远端合并，再从新 main 重建本包。因此 Pro correction count
仍为 0；这是一项 Codex pre-dispatch correction。

## 待完成

- Pro 输出 hash、patch/ZIP/report/log 完整性：`PENDING`
- 隔离 worktree 应用与允许路径审查：`PENDING`
- race/vet/test/build/generate/mod/vuln：`PENDING`
- 中文 PR、远端 CI、squash merge：`PENDING`
- 外部 staging/production：`NOT_AUTHORIZED`
