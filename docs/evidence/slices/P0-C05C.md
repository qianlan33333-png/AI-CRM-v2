# P0-C05C：P0-S03 共享接线

状态：`PR_CI_VALIDATED — PENDING_MERGE`

证据层级：`LOCAL CONTRACT`。本片不实现 `PingStore`，不连接 PostgreSQL，不执行迁移、GitHub、部署或任何真实外部调用。

## 范围与接线

- base：`99509f99cadbdad982c06502d19f51238ac59427`；branch：`codex/p0-c05c-sqlc-shared-wiring`；task：`/root/p0_c05c_sqlc_shared_wiring`。
- executor：`gpt-5.6-terra`，`reasoning_effort=ultra`；worktree：`/Users/qianlan/Downloads/新CRM/handoffs/terra-worktrees/P0-C05C`。
- core payload：`.github/workflows/application-go.yml`、`Makefile`、`scripts/check_repo_contract.sh`、`scripts/test_repo_contract.sh`、`docs/execution/slices/P0-S03.md`；receipt carriers：本页和 `docs/execution/slice-ledger.yml`，二者不参与 core manifest，避免自引用。
- `p0-s03-contract` 总是以 `GOWORK=off GOTOOLCHAIN=local GOFLAGS=-mod=readonly` 运行 C05B gitless `test_contract.sh`。`p0-s03-acceptance` 仅两个实现文件均不存在时才在合同后 PENDING；任一文件、链接或 special 出现即 static fail-closed；完整实现才要求 store `-race -cover` 输出为 100%，再跑 readonly tagged acceptance。
- application Go gate 仅注入固定 P0-S03 loopback 开关与 DSN；不使用 secrets，不改变既有 PostgreSQL 16.14 service、health 或 Goose migration gate。
- checker 精确固定 Makefile/workflow SHA、C05A query contract、C05B 三门、store contract 与 P0-S03 卡，并保留 Make target、合同 recipe 与 `ci-go` 依赖的机械断言；永久 fixture 同时拒绝缺 required 合同文件、缺 acceptance 依赖、缺 `ci-go` 依赖、hollow recipe 与重复 target。

## 既有收口与红队边界

- C05A 已随 PR #16 合并为 `17c006bb67b28c9c19afbf316c1eafa1aefb41ea`；C05B Issue #17 / PR #18 已合并为本片 base。C05B PR：application `31273561488`、repo `31273561479`、secret `31273561476`；main：application `31273630598`（PG16.14 + Goose）、repo `31273630612`、secret `31273630597`。
- 上述 C05B PR/main 仅是本接线前 baseline；P0-S03 shared gate 当时 `NOT_WIRED`，不把它们表述为新 gate 的远端通过；均 `NOT_DEPLOYED`。
- 保留 C05B 红队纠错：拒绝 method-value/FuncLit 与 generated extra/symlink/FIFO/special/mode 绕过；GNU `stat -f` 非八进制输出须 fallback，Bash3 窄 PATH 不得因缺 `rg` fail-open。本片不修改这些门。

## 本地与远端证据

- LOCAL：`bash -n`/YAML、C05B gitless contract（exit 0）、空实现 acceptance（合同后 PENDING，exit 0）、generate ×2、mod/fmt/vet/race/build、离线 `make ci-go`（exit 0）均已通过；P0-S02 与 P0-S03 仍正确 PENDING。
- 无 Git fixture：half、dangling link、FIFO 均在合同后 static fail-closed（各 make exit 2，无 PENDING）；`GOFLAGS='-mod=mod -definitely-invalid'` 的完整夹具仍到达 100% coverage 和 tagged 阶段，仅因缺 PG 开关预期 exit 2，未连接数据库。
- SECURITY：payload-only gitleaks、实际 payload 自定义模式扫描及现有 index custom scan 通过；hostile `GIT_INDEX_FILE` 在任何 fixture 前被明确拒绝（exit 1）；root staged repo checker、permanent fixtures 与 canonical diff 均 PASS。
- PR #20 head `7ba80db890d2547b67f2d7fbc798b5de4616ec03`：application SUCCESS `31276016745/job93149528408` 实证 `p0-s03-contract-tests PASS`、真实 repo `PENDING`、`PG server_version_num=160014` 与 Goose up/down/up；repo-contract SUCCESS `31276016780`；secret SUCCESS `31276016739`。
- core manifest SHA-256：`ead5a69f74fcbd7e15f99f1a8f973ff7290a153b4ec77c26abc4d26b0f241648`；root staged canonical binary diff SHA-256：`06d121db815e139355292ab59132ecac0f0a708eb17ea291455aefb39fce968b`，已验证。receipt carriers 不参与两者。
- PG：`PG_P0_S03_QUERY_NOT_EXECUTED`；PR CI 已验证，merge/main/部署仍 pending，不得由本地结果替代。
- P0-S03 卡保持 `BLOCKED — READY_AFTER_P0-C05C_MERGE` 与 `P0_C05C_MERGE_SHA_PENDING`；仅 C05C 合并后 root 才可写入精确 merge SHA 并派发。
