# P4-B02：本地标签目录 CRUD 旧后端兼容

## 冻结范围与精确销账

- Base 为 exact-green `16d2c34ef869539e3342647f9f1b82a0984df2c1`，从 fresh 隔离 worktree 实现；历史 B02 readiness WIP 只读，未复制或 cherry-pick。
- 仅关闭 `LEGACY-API-0555/0552/0553/0556/0562` 共 `+5`。0553 和 0562 各自包含 PUT/PATCH/DELETE method bundle，PUT/PATCH 共享 update 语义，不重复计数。
- 仅关闭 `LEGACY-S05-010/013/014/015/016/017/018` 共 `+7`；迁移账本 `+0`，`LEGACY-T14-148/309/310` 保持 `PENDING_TARGET_SCHEMA`。

## 实现边界

- 读取与写入只使用 Contact-owned `tag_groups`、`tags` 及既有 transaction-bound UoW；写操作与本地 catalog Event 在同一事务。
- GET 复用 human session 与 `customers.read`，无 CSRF；写路由复用 `customers.write` 与 A01 CSRF。请求中的 actor 不可信，实际 actor 始终来自 session principal。
- 读取失败固定返回 HTTP 200 degraded 空目录；所有响应明确 `fallback_used=false`、`real_external_call_executed=false`、`sync_executed=false`、`fixture_used=false`。
- archive-compatible 投影用 Contact 本地 tombstone 隐藏归档组/标签，不删除 customer_tags 事实，不访问 Identity、WeCom 或其他域表。

## 明确排除

- 无 migration/DDL、tenant model/selector/column/index/RBAC、跨租户测试、UI、sidebar、同步、live gate、批量打标、provider 调用、真实企微或真实外发。
- 客户单标签 Add/Remove 继续复用既有 Contact API/UoW/Event 能力；本片不新增对应路由。

## 修正与验证

- 前次 `slice_induced=2` checkpoint 保留为 `SCOPE_FROZEN`，不是 HARD STOP；中央裁定后同一 worktree 续作。
- `slice_induced=7`：前六项为 SQL CTE/target `id` 歧义、生成 nullable 假设、既有 WeCom 字段负例、method-bundle operationId 唯一性、P4 Tag registry、authorization registry；R1 删除违反明确边界的跨租户负例。第 3 个起严格 repair-only，未增加 tenant 运行时语义；`redline=0`。
- `verification_induced=22`：首个 B02 checkpoint 记录 8 次；B02-R1 append-only repair 记录 11 次，覆盖真实 PR/SHA/method/command 证据、受影响 pinned receipts、禁用模式负例收窄，以及两次从 exact HEAD 恢复历史 ledger 后仅追加 R1；B02-R2 记录 3 次，覆盖 secret-scan 跨行误报分隔符、feature-matrix completion 派生计数 fixture 与其 pinned receipt 同步。`infra_induced=0`。
- 本域 normal/boundary/error、A01 session/RBAC/CSRF、Contact UoW/Event、SQLC focused store、OpenAPI registry、generated、race/vet/static 与受影响 repo 负例均须在候选锁定前通过。
- `PRODUCTION_DATABASE_NOT_EXECUTED`、`LIVE_MIGRATION_NOT_EXECUTED`、`REAL_WECOM_NOT_EXECUTED`、`REAL_SEND_NOT_EXECUTED`、`DEPLOYMENT_NOT_EXECUTED`。
