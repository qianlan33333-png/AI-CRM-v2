# P3-S06：Segment 人群包 UI 完整流

## 输入合同

- Base SHA：`69da552b468eeeabcdb1c9a9bd86f8de39d141fc`，O3 00019 CLOSED 后的 exact-green main。
- 只调用已合的 Segment typed client：list/create/update/member-preview/request-refresh；不修改 OpenAPI、public port、后端语义、DDL、DSL/compiler、River 或 SQL。
- 旧候选 `97031549460bd328643daab1baeecee4e640e696` 仅作行为证据，不复制提交、不 cherry-pick。

## 可观察行为

- admin/ops 可从主导航进入人群包，查看服务端 opaque cursor 列表，创建或编辑已冻结 DSL 条件，并预览已物化成员。
- 条件编辑器只提供既有字段、操作符、AND/OR 与受校验的 ID/时间/删除状态值；不接受 raw JSON、identifier 或 SQL 文本。
- 所有写请求使用 same-origin、CSRF 与每次操作的 Idempotency-Key；稳定状态码映射为可读错误，不显示服务端异常内容。
- 手动刷新只在服务端响应 `202 accepted` 后提示“已持久化入队”；随后重新读取列表和成员预览，不把 accepted 误报为成员已更新。

## 边界与证据

- 手写业务范围：Segment Web transport/parse、页面与样式、Web route/permission；测试、slice card、ledger/contract hash 不计入业务规模。
- 不执行生产数据库、live migration/cutover、真实企微或真实外发；本片无迁移和后端写入。
- 验证包含 strict TypeScript、Web unit/render tests、Web build、生成物/contract/secret gates 与 GitHub required CI。
