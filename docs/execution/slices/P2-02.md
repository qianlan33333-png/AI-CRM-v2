# P2-02：强类型启动配置与全量 fail-fast

## 输入合同

- Base SHA：`c8326ae0cc8a62c8c193b80ab2a72b27db3660a3`
- Phase：`P2`
- 依赖：P2-01R / PR #99 已 squash merge，精确 main CI 全绿
- 执行：Codex-Sol；config schema、composition root 与架构 lint 不委派
- correction：`slice=1 / infra=0 / scope=0 / verification=0`

## 完整行为

1. `internal/config` 是启动环境的唯一读取边界；`cmd/**` 与其他
   `internal/**` 包直接调用 `os/syscall` 环境读取 API 时架构 lint 必须失败。
2. `config.Load(role)` 一次读取并强类型化该 role 会实际使用的全部字段，并把同一个
   typed `Root` 传入 composition root；无默认 fallback，缺失或非法时在任何
   component 启动前拒绝启动。
3. 一次返回所有相关字段问题，顺序稳定且只显示字段名/规则，不回显 DSN、密码或
   原始输入。
4. `--help` 与 CLI usage error 不读取配置；合法 role 才进入配置校验。

## 字段合同

| typed field | environment | role | validation |
|---|---|---|---|
| `database.url` | `AICRM_DATABASE_URL` | api / worker / all | `postgres` 或 `postgresql` URL，host 与 database 必填 |
| `api.listen_address` | `AICRM_HTTP_LISTEN_ADDRESS` | api / all | 合法 `host:port`，端口 1–65535 |
| `api.pool_max_conns` | `AICRM_API_PGX_MAX_CONNS` | api / all | 正整数 int32 |
| `worker.pool_max_conns` | `AICRM_WORKER_PGX_MAX_CONNS` | worker / all | 正整数 int32 |

`DatabaseURL` 使用 opaque typed value，`String`/`GoString` 固定输出
`[REDACTED]`；只有显式 `Value()` 才能交给后续 pgx wiring。

## 验收

- 表驱动单测覆盖三种 role、缺失/非法/聚合错误、URL/地址/整数边界与 redaction。
- 进程黑盒证明：配置缺失/错误退出 1、字段可定位、值不泄露；合法配置下三种 role
  保持既有信号生命周期。
- 架构 lint 永久正例仅允许 `internal/config/load.go` 读取环境；直接读取和 import
  alias 绕过均为永久负例。
- focused tests、全量 Go/Web、连续生成、repo-contract 负例和 secret scan 全绿。

## 明确不做

- 本片不创建 settings/settings_audit，不定义业务设置热更新，不决定 secret 或企微
  凭据存储；这些属于 P2-03 安全边界门。
- 不连接任何数据库，不监听端口，不部署，不读取生产环境或真实凭据。

## 回滚

回滚本片 squash merge 即恢复 P2-01R 的启动骨架；无数据库与外部状态变更。
