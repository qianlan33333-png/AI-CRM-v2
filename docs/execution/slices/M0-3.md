# M0-3: EXPLAIN / Seq Scan CI 门

## Fixed input

- Base SHA: `890e6884f488e1a4b45092e8c3903d4d44b1975a`
- Phase/milestone: `M0`
- slice_kind: governance
- task_inputs:
  - `docs/spec/AI-CRM-v2-执行方案-v2-至P3.md`
  - `docs/spec/AI-CRM-v2-重构详细设计.md`
  - `docs/adr/ADR-009.md`
- Execution: `sol_vertical_slice`; task type: `governance`
- Executor: `gpt-5.6-sol`; reasoning: `session`; task ID: `M0-3`
- Database writes: `CI_TEMPORARY_POSTGRESQL_16_14_ONLY`
- Production/real data/external effects: `NOT EXECUTED / PENDING_EXTERNAL_GATE`

## Goal

当 migration 或 SQLc query 改变时，在独立临时 PostgreSQL 16.14 数据库中以 generic prepared plan 拒绝 customers/customer_events 的 Seq Scan。

## Acceptance criteria

- migration 变更检查全部目标表 SQLc blocks，query 变更仅检查相关 blocks。
- 数据库 URL、Git SHA、SQL 解析、PREPARE 或 EXPLAIN JSON 异常全部 fail-closed。
- 索引查询、无相关查询、无目标表查询通过；`SELECT * FROM customers` 快速非零且输出精确诊断。
- `make ci-go` 与 GitHub Actions 均在固定 PG16.14 环境执行本门。
