# P3-I2：Identity Resolve 显式结果

## 输入与目标

- Base SHA：`d0c32885c9b063068696d9e52570f70e6f702c72`。
- Phase：P3 / identity wave / implementation。
- task_inputs：`docs/spec/AI-CRM-v2-执行方案-v2-至P3.md`、
  `docs/spec/AI-CRM-v2-重构详细设计.md`。
- 已冻结中央契约：P3-I00、ADR-004、ADR-012、
  `migrations/00010_identity_storage.sql`、`internal/identity/port/port.go`；本片只消费，
  不修改它们。
- 目标：将一个合法的七类 IdentityRef 规范化后精确查询，返回 `found`、`not_found` 或
  `conflict`；不产生任何业务写入或隐式客户建档。

## 冻结行为

- 必须复用 I1 的唯一 v1 normalizer；查询只绑定 kind、scope、normalized value，不能以
  fingerprint、raw value 或模糊匹配作为 Resolve 键。
- 不存在 identity 或 floating identity 返回 `not_found`，且 `customer_id` 为零。
- 唯一绑定到未删除 Contact OneID 的 identity 返回 `found` 和该正数 `customer_id`。
- 绑定指向不可用 Contact 父行是可信边矛盾，返回 `conflict` 且不泄露候选 ID。
- Resolve 是只读行为：不新增或更新 `identities`、`customers`、`pending_events`、
  `customer_merges`、receipt 或 `event_log`，不创建客户、不合并、不发事件。

## 路径与验收边界

允许：

- `internal/identity/app/resolve{,_test}.go`
- `internal/identity/store/{repository.go,queries/identities.sql,generated/**}`
- `acceptance/identity/resolve_integration_test.go`
- `acceptance/contactfixture/contactfixture{,_test}.go`（仅 Contact-owned PG 父行夹具）
- `scripts/{generated-sources.sha256,check_generated_sources.sh,check_repo_contract.sh}`
- `docs/execution/{slices/P3-I2.md,slice-ledger.yml}`

明确排除：Bind、Ingest、merge/review/replay、migration、ADR、OpenAPI、公共 port、
HTTP/API、keyring、root dependency、真实企微、生产 DB、live migration/cutover 及任何
旧 Python 源码。

验收使用隔离 PostgreSQL 16.14，覆盖 floating/not_found、有效绑定/found、不可用绑定/
conflict、输入规范化与非法值零创建；Contact 父行只经 Contact-owned fixture 创建或软删。
生产 DB、live migration、真实企微和外发均为 `NOT EXECUTED`。

## 回滚

revert 本 PR 即可；不执行 live migration、真实企微或外发。
