# P1-S01：确定性旧路由导出器

- Issue: #61
- Base SHA: `cd7646560475aedefa897c2dfeca30744ea9396f`
- Executor: Codex Sol vertical Slice
- Source baseline: legacy `origin/main@6cb989c071255437d75953dabb943318a74eb8f4`

## 单一行为

把旧仓 CI 已验证的 `docs/architecture/route_ownership_manifest.yml` 转为排序、
逐字节可重放的 JSON 事实清单。输出固定记录 source SHA、输入 SHA-256 和 route count。
独立静态 AST 审查确认主 registry 与 manifest 为 781/781 逐键匹配；独立 WeCom ingress、
registry 容器 owner 与 route group 不在本产物中，必须由后续 API 盘点单独对账。

## 边界与验收

- 不导入旧 FastAPI app，不启动 runtime，不访问网络、DB、凭据或外部系统。
- 输入只允许不超过 4 MiB 的普通非链接文件和冻结的 YAML 子集；未知字段、畸形类型、
  重复字段/路由、非小写 40 位 source SHA 必须失败。
- `go test -race ./legacy-route-export`、二次导出 byte-for-byte、仓库门、secret scan 和 CI 必须通过。
- 生成产物是 source baseline 证据，不是 production/deployment 证据。
- 回滚：revert 本 Slice；不涉及数据或运行时状态。

## 产物

`docs/evidence/p1/legacy-routes-6cb989c.json` 由本工具生成，禁止手写。

- 输入 manifest SHA-256: `3bb11a48c8bbc520fb9da5128726594232bca0b4e0f0c7ed1f63bb4b3c2263bd`
- 输出: 542,348 bytes / 781 routes
- 输出 SHA-256: `fb6aa066c985af4d0c5e3abe8a33c88b5c3a6a56dda9ee7fd6e51aca8cb5f231`
