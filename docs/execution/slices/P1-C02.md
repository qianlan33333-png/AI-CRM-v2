# P1-C02：迁移映射字段理由与 lifecycle 索引闭环

## 输入合同

- Base SHA: `82b1da04d1d35e56c6aff97c7eb07eb35cea1f59`
- Phase/milestone: `P1 closeout`
- slice_kind: `evidence`
- task_inputs:
  - `docs/migration-mapping.jsonl`
  - `docs/migration-mapping.md`
  - `docs/spec/AI-CRM-v2-执行方案-v2-至P3.md`
  - legacy lifecycle manifest `710a01ee3813051b4ec13de8ef8b8ad64b39bc380b3a5a81c669580df24b488e`
- Execution: `sol_vertical_slice`; task type: `vertical_behavior`
- Executor: `gpt-5.6-sol`; reasoning: `root_session`; task ID: `P1-C02`
- Worktree: `/Users/qianlan/Downloads/新CRM/handoffs/sol-worktrees/M0-SERIAL`
- Dependencies: `P1-S09-S10`, `P1-C01`

## Goal

把 legacy lifecycle manifest 的 316 张表和 3313 个字段处置理由机械闭环，同时保持 #80 的候选 target、decision 与 signoff 不变。

## Frozen contract

- lifecycle index 固定 legacy SHA、manifest SHA、316 个排序且唯一的表记录，以及每表 domain、lifecycle、migration source、source line。
- mapping 表集合与 index 必须完全相等；217 张物理表的 3312 个字段和 1 个 framework metadata 字段都必须恰好映射一次。
- 每个字段理由必须非空，并绑定精确 `table.field` 与 target；不存在只在表级解释的丢弃字段。
- external_userid、unionid、openid、mobile、phone 不能映射到 customers 或其他无关业务目标；进入 identities 时必须同时有 scope 字段、scope conversion rule 与 provenance 理由。
- 315 条 `PENDING_HUMAN_SIGNOFF` 与 1 条 framework `NOT_REQUIRED` 原样保留；本片不生成迁移 SQL。

## Path boundary

允许修改：mapping JSONL/说明、frozen lifecycle index、本 slice card、ledger、migration-mapping validator/tests、repo-contract checker/runner。

禁止：migrations、业务实现、OpenAPI、旧系统写入、真实 DB、外部调用、部署。

## Acceptance criteria

1. frozen validator 输出 `rows=316 physical=217 columns=3312 pending=315`。
2. 永久负例拒绝缺字段、缺理由、伪理由、漏 lifecycle 表、元数据漂移、身份写回 customers/无关目标及缺 scope 的 identity mapping。
3. repo-contract 固定 index、mapping、说明、validator/tests 与 slice card 的普通文件模式和内容收据。
4. focused race、repo-contract/runner、全仓静态门与 CI 通过。

## 外部门

- legacy/生产数据库、真实迁移、部署、企微或其他 provider：NOT EXECUTED
- 人工 migration disposition/signoff：PENDING_EXTERNAL_GATE
