# P3-C01D：Contact 客户列表 HTTP、RBAC 与运行时装配

## 输入合同

- Base SHA：`cb3210f27766cb053f542c4f206c12f02b5b663e`
- Phase：`P3 / contact wave`
- slice_kind：`sol_http_security_boundary`
- 决策锚点：`USER-CONTINUE-THROUGH-P4-2026-08-12`

## 完整行为

1. generated `listCustomers` 参数逐项映射到已冻结的应用服务；成功响应保留 opaque
   cursor、固定 watermark、10k+ 标记与渠道中立 Customer 字段，`extra` 只接受严格
   JSON object，并以 `json.Number` 保持数值精度。
2. handler 只接受 `customers.read` 授权上下文。admin/ops 使用 global scope；sales
   必须使用与 principal staff 完全一致的 owner_staff scope，缺 owner 时由服务端强制
   注入，显式请求其他 owner 时拒绝而不访问应用服务。
3. generated 参数绑定错误与其他筛选校验为 400；非法 cursor 为稳定
   `CURSOR_INVALID`，依赖或应用返回形状异常为 503；错误响应不泄漏底层原因。
4. API runtime 显式装配 transaction-bound UoW、sqlc customer store、应用服务和 contact
   HTTP handler；不新增公共 port，不跨域直写数据。

## 非范围

- 本片不实现客户写入、详情、标签/阶段写、时间线、UI 或 20 万性能结论。
- 不新增外部身份字段，不读取旧 Python，不连接生产数据库，不部署，不执行真实企微
  调用或 live migration。

## 验收

- focused race 覆盖全部参数、global/owner staff 正负路径、cursor/validation/dependency
  错误、非法应用返回与完整成功响应。
- 生成器连续两次无 diff；全量 Go/Web/repo-contract/secret scan。
- 中文 PR、squash merge和精确 merge SHA 的 main CI。

## 回滚

回滚本片 merge；本片无 DDL、持久数据写入或外部效果。

## correction

- `slice_induced=1`：首版把非 cursor 的列表筛选校验映射为 422；Sol 在测试冻结复核时
  发现 listCustomers OpenAPI 只声明 400，改为稳定 `MALFORMED_REQUEST`，未修改冻结契约。
- `infra_induced=0`
- `scope_induced=0`
- `verification_induced=1`：首次调用了仓库不存在的 `make repo-contract` 目标；改用 CI
  实际命令 `scripts/check_repo_contract.sh`，不影响代码或门禁强度。
