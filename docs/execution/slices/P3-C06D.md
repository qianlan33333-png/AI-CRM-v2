# P3-C06D：标签驱动的有界总数查询

## 输入合同

- Base SHA：`5d5b2a4ef00f10f19b42b184d6cbb2a24c1825d2`
- Phase：`P3 / contact wave`
- slice_kind：`query_performance_optimization`
- 决策锚点：`P3-C06C-S-TIER-RERUN-2026-08-12`

## 完整行为

P3-C06C 合并后的授权测试机整轮仍有 78 个场景 P95 `>=200ms`，且全部集中在
`tag` 与 `tag+keyword` 两组。最慢场景中 `CountCustomerIDsBounded` 为
`324.592ms`、页面查询仅 `3.928ms`；有界总数查询沿客户 keyset 索引扫描约
19 万 active 客户，再过滤标签，产生约 57 万 shared-hit blocks。

本片保持一个 `CountCustomerIDsBounded` operation、固定 12 个参数、
`min(total, 10001)`、所有筛选和 count→page 两查询合同不变。tag 为空时保留
原 customers keyset 有界分支；tag 非空时从 `idx_customer_tags_tag` 驱动，
以 lateral customers 主键查找应用同一组客户过滤。两个分支以 tag NULL/NOT
NULL 互斥，禁止重复计数、无上限 COUNT、OFFSET 或目标表 Seq Scan。

## 验收

- sqlc 连续生成两次无 diff，generated manifest 同步。
- unit/real-PG 验证两分支互斥、过滤同构、0/精确 10k/10k+、tag 重复不计数、
  keyset 页面语义不变。
- focused race/vet 与永久合同负例通过。
- 从新的精确 main SHA 重建 generator/runner，在相同 2C4G、PG16.14、20 万
  客户和 4096 场景上从头执行；只有每个组合 P95 `<200ms`、无目标 Seq Scan、
  runner 与离线 verifier 同时 PASS 才关闭 Contact 硬门。

## 规模与回滚

生产手写范围为 SQL 与测试机 runner 共 2 个文件、137 个物理变更行。测试、
sqlc 生成物、卡、
证据与治理不计入。回滚为回退本片 squash merge；无 DDL 或数据迁移。
