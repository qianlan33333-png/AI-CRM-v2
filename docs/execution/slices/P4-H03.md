# P4-H03：群邀请素材库本地 CRUD

## 输入与冻结闭环

- Base 为 exact-green `68ac73f8ede00a38e8564a56763b2084e67b0699`；本片在 fresh worktree/branch
  手工实现。历史 H03 checkpoint/WIP 只读用于 immutable 合同与测试取证，没有复制、
  cherry-pick 或发布旧树。
- 唯一闭环是 `GET/POST /api/admin/group-invite-library` 与
  `GET/PUT/DELETE /api/admin/group-invite-library/{item_id}`；精确承接
  `LEGACY-API-0335/0336/0337/0338/0339`，DELETE 在 v2 core 为 archive。
- 中央 disposition 同步为：P1 冻结的 0335 `MIGRATE`、0336..0339
  `DEFERRED_POST_LAUNCH` 保持不可改的历史决策；本片以 OpenAPI
  `x-p4-decision-evidence: P4-H03-2026-08-15` 显式记录 P4 对五路由的实时授权，
  不篡改 P1 调查清单与 501/268/12 历史计数。

## 领域、数据与原子性

- 冻结字段仅为 `name/title/description/join_url/cover_image_id/enabled`；title 最多
  128 UTF-8 bytes，description 最多 512 bytes，只接受有效
  `https://work.weixin.qq.com/gm/...`。可选 cover 只通过 Media-owned 只读 port
  查已有 image ID，不读 blob，不直查跨域表。
- `00034_media_group_invite_library.sql` 仅新增 Media-owned
  `media_group_invites/media_group_invite_operation_receipts`；单实例、单企业、单 DB，
  无 tenant model/column/index/port/RLS 或跨租户测试。
- create/update/archive 均以 actor + operation + business key + idempotency key 领取 receipt。
  业务事实、completed receipt 与 `media.group_invite_created|updated|archived` Event
  处于同一 platform UoW；同 payload replay、异 payload conflict、actor 隔离、四故障回滚与
  双连接并发均由真实 PG 验收。JSONB receipt 以精确数值语义比较，不比原始字节。

## 验收、销账与排除

- loopback PG16.14 已真实执行 `33→34→33→34`，Event/Auth/Media 历史前缀与逐行指纹保持；
  normal/boundary/error/race、A01 session/capability/RBAC/CSRF 正负、Media 引用 fail-closed、
  replay/conflict/actor isolation、四故障 rollback、并发收敛及 20 万 touched query
  `FORMAT JSON` 无非法 Seq Scan 均通过。
- 本片最终只销账 `+5 API`、`+0 feature matrix`、`+1 LEGACY-T14-183 physical target only`。
  T14-183 保持 `DEFER/NOT_STARTED/NOT_RUN`，新 v2 DDL 不冒充历史生产 import。
- 明确未建设：`pic_url/config_id/provider state/chat binding/provider media refresh/blob/file/upload`、
  `add_join_way`、真实企微、外发、API0332/0333/0334 bindings、H04 resolve/provider、UI、
  tenant 与新语义。

## 修正记账与外部门

- `slice_induced=2`：自审发现首版 idempotency payload digest 误包含系统时间；transport
  负向黑盒发现空 PUT 会进入 stub。两项均只修复原 DoD，第二项后范围永久冻结为
  repair-only。
- `verification_induced=9`：单测 helper 首次与已有名称冲突；缺 CSRF 负例首次未考虑
  A01 same-origin cookie fallback，改为冻结的 cross-origin 负例；首次直接消费者复验发现
  OpenAPI freezer 尚未登记 H03 五个 operation，只同步已冻结的 evidence/mapping/capability/schema；
  独立盲审要求不凭测试名推断，因此将四故障显式扩为 create/update/archive 各12组并加入
  真实 repository 不存在 cover ID 零残留断言，仅增强原 DoD 验证；OpenAPI freezer
  首次定向命令从 root module 调用了独立 tools module 路径，改从 `tools/` 执行；
  fresh worktree 临时 Orval 路径在首次连续校验前已清理，复用已安装的 pinned 7.21.0
  后两次生成无 diff；独立盲审首次直跑 acceptance 漏了 `-args -database-url`
  导致 DB case skip，改用正确公开 runner 后全部真实执行；PR receipt 首次手工回填了
  错误长 SHA，在任何收据提交前以 `git rev-parse HEAD` 替换为精确提交；首次回填尝试
  改动已提交 H03 ledger，append-only 门按设计拒绝，恢复原块后只追加 H03-R1。
- `infra_induced=1`：首次受影响负例命令含临时 fixture 的 `rm -f`，本地工具政策在执行前拒绝；
  改为不含该删除语法的同等临时 fixture 验证，不改业务树。`scope_induced=0`、`redline=0`。
- `PRODUCTION_DATABASE_NOT_EXECUTED`、`LIVE_MIGRATION_NOT_EXECUTED`、
  `REAL_WECOM_NOT_EXECUTED`、`REAL_SEND_NOT_EXECUTED`、`DEPLOYMENT_NOT_EXECUTED`。

## GitHub 交付收据

- 中文 PR：`https://github.com/qianlan33333-png/AI-CRM-v2/pull/216`；初始实现提交：
  `c0aa96caa3c6ee86e012bad3aae4d759de6677a3`。最终以独立脱敏证据门、required CI、
  match-head squash、唯一父/tree 与 exact-main 四门全绿为 CLOSED 证据。
- 手写业务合同为 13 文件/1404 新增行，低于 full behavior 15 文件/1500 行上限。
- 独立脱敏 PRE-PR migration/PG 盲审：`PASS`；不接触 migration/H03 源码完成目录、
  `33→34→33→34`、历史指纹、12 组回滚、cover 只读、并发与 20 万 Index Scan 复核。
