# M0-6：source-policy 基础设施缺陷修复

## 输入合同

- Base SHA: `084e35724743e5782ab1bdcded5dbbc9f76e1549`
- slice_kind: `governance`
- task_inputs:
  - `AGENTS.md`
  - `docs/spec/AI-CRM-v2-执行方案-v2-至P3.md`
  - `scripts/sourcepolicy/main.go`
  - `scripts/check_generated_sources.sh`
- Executor: Codex-Sol; task ID: `M0-6`

## Goal

修复生成物路径识别不全和 `URL.Query()` 被误判为数据库调用的既存门禁缺陷，
且确保任何获得 source-policy 豁免的 `internal/**/generated/*.go` 都被生成物
可复现性清单覆盖。

## Frozen behavior

- 手写 `r.URL.Query()` 必须通过。
- 手写 `db.Query(...)` 在普通路径和 `internal/api/candidate/` 都必须失败。
- `internal/**/generated/*.go` 可跳过手写源码门，但新增生成目录或文件若未进入
  `generated-sources.sha256` 则 `generate-check` 必须失败。
- 修正分为 `slice_induced` 和 `infra_induced`；仅前者触发规模降档，两者之和
  达到 2 仍须停报。

## 自查结论

source-policy 其余 selector 规则已对 `os/syscall` 和 `time` 做 import alias 接收者
约束；动态 SQL 与 cron 基于 import path；未发现需扩片的第三个同类确定缺陷。

## 外部门

- 生产、部署、真实企微、真实数据库与迁移：`NOT EXECUTED`
