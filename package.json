{
  "name": "ai-crm-v2-web",
  "private": true,
  "version": "0.0.0",
  "description": "AI-CRM v2 TypeScript frontend — admin console, WeCom sidebar and H5",
  "type": "module",
  "engines": {
    "node": "24.18.0",
    "npm": "11.12.1"
  },
  "packageManager": "npm@11.12.1",
  "scripts": {
    "version:check": "node -e \"const ua=process.env.npm_config_user_agent||'';if(process.version!=='v24.18.0'||!ua.startsWith('npm/11.12.1 ')){console.error('expected node v24.18.0 and npm 11.12.1');process.exit(1)}\" && test \"$(orval --version)\" = \"7.21.0\" && test \"$(prettier --version)\" = \"3.9.6\"",
    "orval:generate": "make generate-orval",
    "orval:check": "make orval-check",
    "orval:contract": "scripts/test_orval_generated_check.sh",
    "lint": "tsc -p web/tsconfig.json --noEmit",
    "typecheck": "tsc -p web/tsconfig.json --noEmit",
    "transport:contract": "node web/scripts/transport-contract.mjs",
    "admin:adapter:contract": "node web/scripts/admin-adapter-contract.mjs",
    "capabilities:contract": "node web/scripts/capabilities-contract.mjs",
    "smoke": "node web/scripts/smoke.mjs",
    "e2e": "npm run build --silent && node web/scripts/e2e.mjs",
    "test": "npm run transport:contract && npm run admin:adapter:contract && npm run capabilities:contract && npm run smoke && npm run e2e",
    "build": "node web/scripts/build.mjs",
    "dev": "node web/scripts/dev.mjs",
    "audit": "npm audit --audit-level=high",
    "ci": "npm run version:check && npm run orval:check && npm run orval:contract && npm run lint && npm run typecheck && npm test && npm run build && npm run audit"
  },
  "dependencies": {
    "qrcode-generator": "1.5.2"
  },
  "devDependencies": {
    "esbuild": "0.24.0",
    "jsdom": "30.0.1",
    "orval": "7.21.0",
    "prettier": "3.9.6",
    "typescript": "6.0.3"
  },
  "overrides": {
    "js-yaml": "4.3.1",
    "lodash": "4.18.1"
  }
}
